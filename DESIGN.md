# 디자인 지침

Task 5(지도 화면)와 Task 6(상세 페이지) 작업 시 이 문서를 따른다.
색상값, 서체, 간격은 여기서 정한 것만 사용한다. 임의로 추가하지 않는다.

---

## 설계 명제

> **이 화면에서 색은 오직 가격을 뜻한다.**

**UI 크롬, 텍스트, 아이콘은 전부 무채색이다.**
채도가 있는 색은 필지의 가격 단계에만 쓴다. 선택 상태조차 색이 아니라 **먹선 굵기**로 표현한다.

이유: 이 서비스가 파는 것은 가격 정보 하나다. 색을 여기저기 쓰면 사용자 눈이 어디를 봐야 할지 모른다.
경쟁 서비스(밸류맵, 디스코)가 어렵다는 평을 듣는 이유가 정확히 이것이다.

### 베이스맵은 예외다 — VWorld `Base`(컬러)를 쓴다

처음에는 무채색 `white` 타일을 썼다. 가격 램프와 충돌이 전혀 없다는 점에서는 이상적이었으나,
실제로 띄워보니 **강·도로·지명·시설이 모두 사라져 "내 땅이 어디쯤인지" 알아볼 수 없었다.**

주 사용자는 상속받은 땅의 위치조차 정확히 모르는 사람이다.
그들에게는 양평역·군청·강 같은 지형지물이 좌표보다 중요한 단서다. 그래서 컬러를 쓴다.

대신 배경이 필지를 이기지 않도록 **채도만 살짝 눌러** 균형을 잡는다.
완전히 빼면(`-1`) 컬러로 바꾼 의미가 사라지므로 `-0.25` 정도만 적용한다.

네이버·카카오 지도는 쓸 수 없다. 자체 SDK로만 지도를 제공하며
타일을 뽑아 다른 라이브러리에 붙이는 것은 약관 위반이다.
OSM 공식 타일 서버도 실서비스 사용이 금지된다.

```js
sources: {
  base: {
    type: 'raster',
    // tiletype: Base(컬러) / white(백지도) / midnight / Hybrid / Satellite
    tiles: [`https://api.vworld.kr/req/wmts/1.0.0/${KEY}/Base/{z}/{y}/{x}.png`],
    tileSize: 256, minzoom: 5, maxzoom: 18,
    attribution: '<a href="https://www.vworld.kr">VWorld</a>'
  }
},
layers: [{
  id: 'base', type: 'raster', source: 'base',
  // 배경이 필지 색을 이기지 않을 만큼만 누른다
  paint: { 'raster-saturation': -0.25, 'raster-contrast': -0.05 }
}]
```

**CSS `filter: grayscale()`을 쓰지 말 것.**
MapLibre는 베이스맵과 필지 폴리곤을 **같은 WebGL 캔버스 하나에** 그린다.
캔버스에 CSS 필터를 걸면 타일뿐 아니라 **가격 램프 색까지 함께 죽어** 이 문서의 설계 명제가 무너진다.
채도 조정은 반드시 래스터 **레이어의 paint 속성**으로 한다.

구현 시 주의 세 가지:
- **워커를 직접 서빙해야 한다.** MapLibre는 GeoJSON 파싱을 웹 워커에서 하는데,
  Next.js 번들을 거치면 워커 경로가 깨져 **필지만 조용히 사라진다**(배경지도는 정상).
  워커 파일을 `public/`으로 복사하고 `setWorkerUrl()`로 가리킬 것
- 타일 좌표가 **`{z}/{y}/{x}` 순서**다. 표준 XYZ(`{z}/{x}/{y}`)와 x·y가 뒤바뀌어 있다
- 타일 상한이 z18이다. `maxzoom: 18`을 명시하면 그 이상에서는 확대해 쓴다.
  배경만 약간 흐려지고 필지 폴리곤은 벡터라 선명하게 유지된다

---

## 색상

```
먹     --ink        #1C1C1A   본문, 제목, 선택 테두리
회     --ink-mid    #6B6B66   보조 텍스트, 라벨
연회   --ink-soft   #9A9A93   힌트, 단위 표기
지     --paper      #FAFAF7   화면 배경
면     --surface    #FFFFFF   패널 배경
선     --line       #E3E1DA   1px 구분선
```

### 가격 램프 (5단계)

```
1  --p1  #E9F0EB   가장 저렴
2  --p2  #BCD5C6
3  --p3  #86B29A
4  --p4  #4E866D
5  --p5  #234F3D   가장 비쌈
```

- 단일 색상 램프다. **무지개 팔레트 금지.** 순서 정보가 사라진다
- 구간은 양평군 전체 ㎡당 가격의 분위수(quantile) 5분할로 나눈다. 등간격 분할 금지 — 지가 분포는 극단적으로 치우쳐 있어 등간격으로 나누면 대부분이 1단계에 몰린다
- 가격이 `null`인 필지는 `#F0EEE8` 사선 패턴 또는 단색으로 칠하고 범례에 "정보 없음"으로 표기
- `--p4`, `--p5` 위에는 흰색 텍스트, `--p1`~`--p3` 위에는 `--ink`

### 금지

- 강조색(accent) 추가 금지. 링크는 밑줄, 선택은 2px 먹선으로 표현한다
- 경고·주의를 노랑/빨강으로 표시하지 않는다. 얇은 테두리 박스 + `--ink-mid` 텍스트로 충분하다
- 그라디언트, 그림자, 글로우 전부 금지

---

## 서체

```css
--font-ui:   'Pretendard Variable', Pretendard, -apple-system, sans-serif;
--font-num:  'Nanum Myeongjo', 'Noto Serif KR', serif;
```

**숫자와 지번에만 명조를 쓴다.** 나머지는 전부 Pretendard.

이유: 토지대장, 등기부등본 같은 공적 장부가 명조로 인쇄된다.
공시지가는 시세가 아니라 **공적으로 고시된 가격**이고, 서체로 그 성격을 전달한다.
장식이 아니라 정보의 출처를 나타내는 장치다.

명조를 쓰는 곳은 세 군데뿐이다.
1. 공시지가 총액
2. 지번 (`245-7`)
3. 상세 페이지 h1

나머지 숫자(면적, 단가, 실거래 평균)는 Pretendard + `font-variant-numeric: tabular-nums`.

### 타입 스케일

| 용도 | 크기 | 굵기 | 서체 |
|---|---|---|---|
| 총액 | 34px | 400 | 명조 |
| 페이지 제목 | 24px | 400 | 명조 |
| 지번 | 20px | 400 | 명조 |
| 본문 | 16px | 400 | UI |
| 라벨 | 14px | 400 | UI |
| 단위·힌트 | 13px | 400 | UI |

**14px 미만을 쓰지 않는다.** 상속 토지 소유자가 주 사용자라 40~60대 비중이 높다.
같은 이유로 터치 타깃은 최소 44px, 본문 `line-height: 1.7`.

굵기는 400과 500 두 가지만 쓴다. 700은 쓰지 않는다.

---

## 시그니처: 필지 실루엣

**상세 패널 상단에 그 필지의 실제 모양을 SVG로 그린다.**

```
┌──────────────────┐
│      ╱▔▔▔╲       │   72×72px
│     │     │      │   해당 필지 geometry를
│     ╲__╱▔        │   viewBox에 맞춰 정규화
└──────────────────┘
```

- 채움: 그 필지의 가격 단계 색
- 테두리: `--ink` 1px
- geometry는 이미 API 응답에 있으므로 추가 요청 불필요
- MultiPolygon이면 가장 큰 폴리곤만 사용

땅은 하나하나 모양이 다르다. 아파트 동호수와 결정적으로 다른 점이고,
소유자는 자기 땅을 **모양으로 알아본다.** 이 서비스를 기억하게 만드는 요소다.

이것 말고 다른 장식은 넣지 않는다. 대담함은 여기 한 곳에만 쓴다.

---

## 레이아웃

### 데스크톱 (≥1024px)

```
┌──────────┬─────────────────────────────┐
│          │                    [레이어] │
│  검색     │                             │
│  ────    │          지도                │
│  실루엣   │                             │
│  주소     │                             │
│  총액     │                             │
│  ────    │  ┌────────┐                 │
│  상세     │  │ 범례    │                 │
│          │  └────────┘                 │
└──────────┴─────────────────────────────┘
   320px            나머지 전부
```

지도가 화면의 75% 이상을 차지한다. 패널 폭은 320px 고정.

### 모바일 (<768px)

지도 전체 화면 + 하단 시트. 스냅 포인트 3단계.

```
접힘(88px)   지번 + 총액만
중간(45vh)   실루엣 + 주요 정보
펼침(85vh)   전체
```

필지를 클릭하면 중간 단계로 열린다. 지도를 가리지 않는 게 중요하다.

---

## 패널 정보 순서

이 순서를 바꾸지 않는다.

1. **필지 실루엣**
2. 주소 — `경기 양평군 서종면` (14px 회색) / `문호리 245-7` (20px 명조)
3. **공시지가 총액** — 34px 명조. 화면에서 가장 큰 숫자
4. 기준연도 — `2026년 공시` (13px 연회)
5. ㎡당 / 평당 단가 — 한 줄, 13px
6. 구분선
7. 지목 · 면적 — 라벨/값 2열 (용도지역은 MVP 범위 밖)
8. 구분선
9. 이 지역 실거래 — **아래 문구를 반드시 포함**

```
서종면 최근 3년 토지 거래
평균 ㎡당 421,000원 · 17건

이 필지의 거래 기록이 아닙니다.
정부가 지번을 일부만 공개해 지역 평균으로만 보여드립니다.
```

이 안내를 생략하면 사용자가 자기 땅이 그 값에 팔렸다고 오해한다. 크기는 작게, 문구는 반드시.

---

## 문구 규칙

- 전문용어는 괄호로 푼다. `지목: 전(밭)`, `지목: 임(산)`
- 면적은 평 병기. `1,652㎡ (500평)`
- 금액은 한국식. `3억 2,180만원` — `321,800,000원` 아님
- 문장형 종결 대신 명사형. 버튼은 동사로. `검색`, `지도로 돌아가기`
- 문장부호로 끝내지 않는다 (라벨·버튼). 안내 문장은 마침표를 찍는다

### 빈 상태 / 에러

```
검색 결과: "'서종면 999-9'에 해당하는 필지가 없습니다. 지번을 확인해 주세요"
로딩 실패: "필지 정보를 불러오지 못했습니다. 다시 시도" (버튼 포함)
```

줌이 부족할 때는 **아무 안내도 띄우지 않는다.** 지도를 덮는 배너가 거슬린다.
필지가 사라지는 것만으로 확대해야 한다는 신호가 된다.

사과하지 않는다. 무엇이 일어났고 무엇을 하면 되는지만 쓴다.

---

## 지도 표현

- 필지 채움 투명도 `0.72`. 밑의 지형이 비쳐야 위치를 파악할 수 있다
- 필지 경계선 `#FFFFFF` 0.5px. 먹선으로 하면 축소 시 화면이 새까매진다
- 선택된 필지: `--ink` 2px 테두리 + 투명도 `1.0`
- 호버: 테두리 1px `--ink`, 커서 pointer
- 범례는 좌하단 고정, 가로 램프바 + 양끝 값 표시
- 줌 15 미만: 폴리곤을 그리지 않는다 (안내 문구도 띄우지 않는다)

---

## 품질 기준

- 375px 폭에서 가로 스크롤이 없다
- 키보드 Tab 이동 시 포커스 링이 보인다 (`outline: 2px solid var(--ink)`)
- `prefers-reduced-motion` 존중. 시트 애니메이션 제외
- 다크모드는 구현하지 않는다
- 애니메이션은 하단 시트 전환과 패널 페이드인 두 가지만. 그 외 금지
