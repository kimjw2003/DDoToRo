# 디자인 지침 (3차)

화면 작업 시 이 문서를 먼저 읽는다. 색상값·서체·간격은 여기서 정한 것만 쓴다. 임의로 추가하지 않는다.
구현체는 `web/app/globals.css`이고, 이 문서와 값이 다르면 **globals.css가 맞다.**

이전 지침은 `DESIGN-v2.md` / `DESIGN-v1.md`로 남겨 뒀다 — **참고용이며 기준이 아니다.**
3차 시스템을 눈으로 확인하려면 `design/design-system-v3.html`을 연다.

## 2차에서 무엇이 바뀌었나

2차는 **의도적으로 딱딱한** 시스템이었다 — 라운드 금지, 그림자 금지, 숫자는 명조(Hahmlet).
토지대장·등기부등본 같은 공적 장부의 인상을 형태로 전달하려는 설계였다.
3차는 직방·다방과 같은 **부드러운** 화면으로 간다.

| | 2차 | 3차 |
|---|---|---|
| 서체 | Hahmlet + IBM Plex Sans KR + IBM Plex Mono | **Pretendard Variable 하나** |
| 형태 | 라운드 금지 | **라운드 5단** |
| 구획 | 1px 실선 | **엘리베이션 3단 + 여백** |
| 지도 화면 | 좌측 384px 고정 레일 | **지도 전면 + 떠 있는 카드** |
| 강조색 | 벽돌 1색, 화면당 3회 | **벽돌 1색 9단 사다리** |

### 왜 Pretendard인가

경쟁 서비스가 실제로 로드하는 서체를 확인한 결과다.

| 서비스 | 서체 |
|---|---|
| 직방 | `zuix2.zigbang.io/font/pretendard-subset.css` → **Pretendard** |
| 다방 | `static.dabangapp.com/…/pretendardvariable-dynamic-subset.min.css` → **Pretendard Variable** |
| 호갱노노 | `font-family: 'Pretendard'` |
| 디스코 | Gmarket Sans + Noto Sans KR + NanumSquareRound **혼재** |

부드러운 쪽 셋은 전부 **한 서체 + 한 강조색 + 틴트 사다리**로 수렴했고,
서체가 뒤섞인 디스코만 "어렵다"는 평을 듣는다.

---

## 설계 명제 3개

셋이 충돌하면 위에 있는 것이 이긴다.

### 1. 채도는 가격만 뜻한다 — 유지

UI 크롬·텍스트·아이콘은 전부 무채색이다. 채도가 있는 색은 **가격 램프 · 주 버튼 · 토글 켜짐 · 선택 상태**뿐이고,
강조색은 램프 최상단(`--p5`)과 같은 색족이라 화면에 색 이야기가 하나만 존재한다.

이유: 이 서비스가 파는 변수는 가격 하나다. 직방·다방이 색을 여러 개 쓰는 것은
카테고리가 여러 개이기 때문이지 부드러워서가 아니다.

지하철·공공시설·뉴스에 **카테고리 색을 배정하지 않는다.**
1.7px 단선 아이콘(`currentColor`)으로 구분한다.

**3차에서 완화된 것 하나** — 2차의 "강조색은 화면당 셋뿐" 규칙은 틴트 사다리가 생기면서 이렇게 바뀐다.

```
채도 높은 단 (500~700)  주 버튼 · 토글 켜짐 · 선택 상태에만
채도 낮은 단 (50~200)   바탕으로 자유롭게 — 채도가 거의 없어 가격 신호로 오독되지 않는다
```

### 2. 위계는 굵기로 만든다 — 2차 명제를 대체한다

2차의 "숫자는 명조로 인쇄된다"를 폐기한다. 서체는 Pretendard 하나이고
위계는 **크기와 굵기(400·500·600·700)**로만 만든다.
고유번호(PNU) 19자리도 모노가 아니라 Pretendard + `.tnum`으로 조판한다.

서체를 하나로 두겠다는 결정을 PNU 한 곳 때문에 깨지 않는다.

### 3. 모양으로 알아본다 — 유지

필지 실루엣이 유일한 시그니처다. 땅은 하나하나 모양이 다르고 소유자는 자기 땅을 모양으로 알아본다.
아파트 동호수와 결정적으로 다른 점이다. 대담함은 여기 한 곳에만 쓴다.

---

## 색

### 무채색 (웜 뉴트럴 — 벽돌 강조색과 짝이 맞는다)

| 토큰 | 값 (oklch) | 용도 |
|---|---|---|
| `--ink` | `20% 0.018 70` | 본문, 제목, 선택 테두리 |
| `--ink-mid` | `48% 0.012 70` | 보조 텍스트, 라벨 |
| `--ink-soft` | `56% 0.012 70` | 힌트, 단위 |
| `--paper` | `98% 0.004 95` | 화면 배경 |
| `--surface` | `100% 0.002 95` | 카드 배경 |
| `--line` | `90% 0.006 95` | 입력 테두리 |
| `--line-soft` | `94% 0.005 95` | 카드 안 목록 구분선 |
| `--line-strong` | `82% 0.008 95` | 강한 구분 |
| `--sunken` | `96% 0.005 95` | 배지 바탕, 탭 트랙, note |
| `--overlay` | `20% 0.018 70 / 0.06` | 카드 위 hover |

### 강조색 — 색상(hue) 28 고정, 9단

**600·700은 2차 값 그대로다.** 브랜드 마크 오른쪽 획(`#9A5048`)·OG 이미지·램프 최상단과
같은 색족이어야 하므로 **이 두 단은 바꾸지 않는다.**

| 토큰 | 값 | 용도 |
|---|---|---|
| `--accent-50` | `97.5% 0.007 28` | 선택 행 배경 · 총액 블록 바탕 |
| `--accent-100` | `94% 0.016 28` | 배지 바탕 |
| `--accent-200` | `88% 0.045 28` | hover · 테두리 |
| `--accent-300` | `80% 0.070 28` | — |
| `--accent-400` | `70% 0.095 28` | 입력 포커스 테두리 |
| `--accent-500` | `60% 0.105 28` | — |
| `--accent-600` | `52% 0.100 28` | **주 버튼 · 토글 켜짐** (= 2차 `--accent`) |
| `--accent-700` | `44% 0.110 28` | 버튼 hover (= 2차 `--accent-ink`) |
| `--accent-800` | `36% 0.090 28` | 저채도 바탕 위 글자 |
| `--accent-900` | `28% 0.065 28` | — |

`--accent` / `--accent-ink`는 600 / 700의 별칭으로 남아 있다 — 기존 참조가 깨지지 않는다.

**50·100의 채도를 올리지 말 것.** `0.012 / 0.024`로 뒀더니 웜 뉴트럴 바탕 위에서
벽돌이 아니라 **분홍**으로 읽혔다. 큰 면적을 칠하는 단이라 눈에 띄게 다르다.

### 가격 램프 5단계 — 2차에서 바뀌지 않았다

| 토큰 | 값 | 구간 (원/㎡) |
|---|---|---|
| `--p1` | `95% 0.022 48` | ~ 32,600 |
| `--p2` | `88% 0.055 42` | ~ 72,800 |
| `--p3` | `78% 0.092 38` | ~ 162,800 |
| `--p4` | `64% 0.120 32` | ~ 461,200 |
| `--p5` | `46% 0.110 28` | 461,200 ~ |
| `--p-none` | `92% 0.004 95` | 정보 없음 |

- 초록이 아니라 점토·벽돌인 이유는 VWorld `Base` 타일의 산림 초록과 램프가 같은 색상이면
  z13~14에서 필지와 지형이 섞이기 때문이다. 벽돌 계열은 누런 육지·초록 산림·파란 물 어디와도 겹치지 않는다
- 단일 색상 램프다. **무지개 팔레트 금지** — 순서 정보가 사라진다
- 구간은 **분위수 5분할**. 등간격 금지 — 지가 분포가 극단적으로 치우쳐 있다.
  값은 `web/lib/priceRamp.ts`의 `PRICE_BREAKS`에 있고 **적재 범위가 바뀌면 반드시 다시 계산한다**
- 가격이 `null`인 필지는 **45° 사선 해치**(`.hatch-none`)로 칠한다. 색을 하나 더 늘리지 않는다
- `--p4`·`--p5` 위에는 흰 글씨, `--p1`~`--p3` 위에는 `--ink`

### 금지

- 경고·주의를 노랑/빨강으로 칠하지 않는다. `--sunken` 바탕 + `--ink-mid` 텍스트로 충분하다
- 그라디언트·글로우 금지. 깊이는 엘리베이션 3단으로만 만든다
- **테두리와 그림자를 함께 쓰지 않는다.** 카드는 그림자로 바탕에서 떼어 놓는다 —
  선을 같이 쓰면 다시 딱딱해진다. 선은 입력 테두리와 목록 구분선에만

---

## 서체

```css
--font-ui: "Pretendard Variable", "Pretendard", -apple-system,
           "Apple SD Gothic Neo", "Malgun Gothic", sans-serif;
```

`--font-num` / `--font-mono`와 `.font-serif-num` / `.eyebrow` 클래스는 3차에서 **삭제됐다.**
남은 것은 `.tnum`(tabular-nums)뿐이다 — 서체가 하나여도 자릿수 흔들림은 막아야 한다.

### 조달 — 자체 호스팅

Pretendard는 Google Fonts에 없다. `npm i pretendard` 후 `node_modules`에서
`public/fonts/`로 복사한다. 스크립트는 `package.json`의 `copy:fonts`이고
`predev` / `prebuild`가 실행한다 (`copy:maplibre`와 같은 패턴).

```html
<link rel="stylesheet" href="/fonts/pretendard/pretendardvariable-dynamic-subset.css">
```

- **동적 서브셋인 것이 핵심이다.** 92개 `@font-face`가 `unicode-range`로 쪼개져 있어
  브라우저가 화면에 실제로 뜬 글자의 청크만 받는다. 디스크에는 3.1MB가 있지만
  한 페이지 전송량은 보통 40~80KB다. **통짜 variable(woff2 1.1MB)로 바꾸지 말 것**
- `public/fonts/`는 생성물이라 gitignore 대상이다
- `layout.tsx`의 `<link>`를 globals.css의 `@import`로 옮기지 말 것 —
  Tailwind가 전개한 규칙 뒤로 밀려 CSS 스펙 위반이 되고 발견 시점도 한 왕복 늦어진다
- **OG 이미지는 별도 경로다.** satori는 woff2를 읽지 못한다.
  `web/assets/Pretendard-{Regular,Bold}.subset.woff`(각 ~340KB)를 커밋해 두고
  `readFile(join(process.cwd(), "assets/…"))`로 읽는다.
  경로를 **리터럴로 적어야** 빌드의 파일 추적이 함수 번들에 넣는다.
  `fetch(new URL(…, import.meta.url))`은 Turbopack에서 `file:` URL로 풀려 실패한다

### 타입 스케일

클래스는 `globals.css`에 있다. 임의 크기를 만들지 말고 이것만 쓴다.

| 용도 | 클래스 | 크기 | 굵기 | 자간 |
|---|---|---|---|---|
| 공시지가 총액 | `.t-amount` | 40px | 700 | -0.03em |
| 페이지 제목 | `.t-title` | 30px | 700 | -0.028em |
| 지번 | `.t-jibun` | 22px | 600 | -0.02em |
| 섹션 제목 | `.t-section` | 17px | 600 | -0.01em |
| 본문·탭·값 | (기본) | 16px | 400 / 500 | -0.005em |
| 라벨·단위·안내 | `.t-label` | 14px | 400 / 500 | 0 |

- **14px 미만을 쓰지 않는다.** 주 사용자가 40~60대다.
  직방·다방은 10~13px을 흔히 쓰지만 하한을 따라 내리지 않는다.
  유일한 예외는 지도 출처 표기(`.maplibregl-ctrl-attrib`, 12px)다
- 굵기는 400 / 500 / 600 / 700. **2차의 "700 금지"는 해제됐다** — 명조가 빠진 자리를 메운다
- 한글 산스는 큰 크기에서 자간을 살짝 좁혀야 뭉치지 않는다. 위 표의 자간을 지킨다
- 본문 `line-height: 1.7`, 30px 이상 표제는 1.05~1.15
- 클릭 타깃 최소 44px

---

## 라운드 · 엘리베이션

값을 임의로 늘리지 말고 이 다섯 단·세 단만 쓴다.

```css
--r-xs:   6px;    /* 배지, 인라인 태그 */
--r-sm:  10px;    /* 버튼, 입력, 세그먼트 */
--r-md:  14px;    /* 지도 위 가격 칩, 범례, 줌 컨트롤 */
--r-lg:  18px;    /* 검색 카드, 패널 카드 */
--r-full: 999px;  /* 토글, pill 토스트 */
```

```css
--e1: 0 1px 2px  oklch(20% .018 70 / .05), 0 1px 3px  oklch(20% .018 70 / .06); /* 가격 칩 */
--e2: 0 2px 4px  oklch(20% .018 70 / .04), 0 4px 12px oklch(20% .018 70 / .08); /* 검색 카드·범례·토스트 */
--e3: 0 4px 8px  oklch(20% .018 70 / .05), 0 12px 32px oklch(20% .018 70 / .12); /* 패널 카드 */
```

그림자는 순수 검정이 아니라 `--ink`의 웜 뉴트럴이다.
**검정 그림자는 회색으로 떠서 차갑게 읽히고 벽돌 강조색과 어긋난다.**

`overflow: hidden` 카드 안에서는 바깥으로 뻗는 포커스 링이 잘린다 —
`globals.css`가 `.card :focus-visible`에 `outline-offset: -2px`를 걸어 안으로 그린다.

---

## 시그니처: 필지 실루엣

상세 상단에 그 필지의 실제 모양을 SVG로 그린다. **88×88px**, 단독 페이지에서는 104px.

- 채움: 그 필지의 가격 단계 색 (정보 없음이면 사선 해치)
- 테두리: `--ink` 1.25px
- geometry는 이미 API 응답에 있으므로 추가 요청 불필요
- MultiPolygon이면 가장 큰 폴리곤만 사용
- 정사각 viewBox에 정규화하되 종횡비는 유지한다 (찌그러뜨리면 모양 식별이 깨진다)

이것 말고 다른 장식은 넣지 않는다.

---

## 브랜드 마크 — 3차에서 바뀌지 않았다

확정 마크는 **ㄸ · 먹 + 벽돌 · 라운드**다. 규격·색·운용 규칙은 `DESIGN-v2.md`의
'브랜드 마크' 절이 그대로 유효하다. 요약만 옮긴다.

```
viewBox        0 0 32 32
각 ㄷ          11.6 × 20 · 획 두께 3.6
모서리·획 끝   round (반지름 1.8 = 획 두께의 절반)
두 획 사이 틈  3.6 — 획 두께와 같다. 이 비례를 바꾸지 않는다
색             왼쪽 #1B150D(ink) · 오른쪽 #9A5048(accent)
```

- 가로획이 12.4 / 27.6에서 끊기는 것은 **의도된 값이다.** 둥근 캡이 1.8을 더 뻗어
  원래 폭 14.2 / 29.4에 정확히 닿는다. 캡을 butt로 바꾸면 마크가 짧아진다
- **에셋 SVG의 색은 hex로 굳혀 두었다.** 파비콘·OG는 브라우저 크롬이나 외부 크롤러가
  렌더하므로 `oklch()`나 CSS 변수를 넣으면 색이 빠진다
- **워드마크는 이미지로 만들지 않는다.** HTML로 조판한다.
  3차에서 이름은 명조가 아니라 **Pretendard 600**이다

---

## 레이아웃 — 데스크톱 전용

```
┌──────────────────────────────────────────────────────┐
│  ╭────────────────────╮            ╭──────────────╮  │
│  │ ㄸ  DDoToRo         │            │ [실루엣] 서종면│  │
│  │ ╭────────────────╮ │            │        245-7  │  │
│  │ │ 🔍 지번 검색     │ │            │ 3억 2,181만원 │  │
│  │ ╰────────────────╯ │            │ ─────────────│  │
│  │  (결과가 여기 펼쳐짐) │            │ 땅정보 시세 주변│  │
│  ╰────────────────────╯            │  (내부 스크롤) │  │
│                                    ╰──────────────╯  │
│  ╭────────────╮                             ╭─────╮  │
│  │ ㎡당 공시지가 │                             │  +  │  │
│  ╰────────────╯                             ╰─────╯  │
└──────────────────────────────────────────────────────┘
    360px (e2)        지도 전면 (z-0)        400px (e3)
```

- 지도가 `.app-shell` 전체를 채운다. 검색·패널·범례는 그 위에 뜬다
- **검색 카드** 좌상단 `top-4 left-4` · 360px · `--r-lg` · `--e2`.
  로고 행 + 입력 + 결과 목록이 **한 카드 안**에서 아래로 펼쳐진다.
  아래를 `bottom-[128px]`로 잘라 결과가 길어져도 좌하단 범례를 덮지 않는다
- **패널 카드** 우측 `top-4 right-4 bottom-4` · 400px · `--r-lg` · `--e3` · `overflow-hidden`.
  **선택 전에는 카드 자체를 렌더하지 않는다** — 떠 있는 카드에서 빈 안내문은 지도만 가린다.
  빈 상태 문구("지도에서 땅을 눌러 보세요")는 검색 카드가 맡는다
- **범례** 좌하단 `bottom-6 left-4` · `--r-md` · `--e2`
- **줌 컨트롤은 우하단이다.** 우상단은 패널 카드와 정면으로 겹친다.
  버튼을 MapLibre 기본 29px에서 **44px**로 키운다 (클릭 타깃 하한)
- **패널이 열리면 우하단 컨트롤을 왼쪽으로 민다.** 패널 카드가 오른쪽 전체 높이를 차지해
  줌과 출처 표기를 통째로 덮기 때문이다(실측: 패널 x 1024~1424, 줌 x 1386~1430 → 완전히 가려짐).
  `MapView`가 `--map-ctrl-right`를 넘기고 `.maplibregl-ctrl-bottom-right`가 받는다.
  **이 규칙에는 `!important`가 필요하다** — `maplibre-gl.css`가 `ParcelMap.tsx`에서 import 되어
  번들에서 `globals.css`보다 뒤에 오고, 같은 명시도의 `right: 0`이 이긴다
- **최소 캔버스 1280px.** 그보다 좁으면 축소하지 않고 가로 스크롤한다 (`.app-shell`).
  1280px에서도 카드 둘 사이에 지도가 480px 남는다
- 모바일 하단 시트는 이번 범위 밖이다. **지도 화면에** `@media`로 패널을 접는 코드를 작성하지 말 것

### 떠 있는 카드가 필지를 가리는 문제

카드 아래 필지는 클릭할 수 없고, 우측 끝을 누르면 방금 고른 필지가 곧바로 패널에 덮인다.
`ParcelMap.tsx`가 둘 다 처리한다 — 값을 한쪽만 고치지 말 것.

```ts
const SEARCH_ZONE = 376;  // left-4(16) + 360
const PANEL_ZONE  = 416;  // right-4(16) + 400

flyTo({ …, padding: { left: SEARCH_ZONE, right: PANEL_ZONE, top: 0, bottom: 0 } })
nudgeIntoView(m, e.point.x)   // 클릭 지점이 카드 영역이면 panBy로 밀어낸다
```

**예외 하나 — `/land/[pnu]` 단독 페이지.**
검색 유입이 목적이라 모바일 검색 트래픽이 그대로 들어온다. 앱 화면이 아니라 공개 문서다.
좁은 폭에서 레이아웃이 깨지지 않을 정도의 폴백(`max-width: 560px` 한 블록)만 유지한다.
**모바일 UI를 설계하는 것이 아니다.**

---

## 상세 패널 구조

**고정 요약 카드 + 3탭.** 총액·지번·실루엣을 탭 밖에 고정하는 것이 핵심이다 —
어느 탭에 있어도 어느 땅인지 잃지 않는다.

**고정 요약** (순서를 바꾸지 않는다)
1. 필지 실루엣 88px
2. 주소 — `경기 양평군 서종면` (`.t-label` `--ink-mid`) / `문호리 245-7` (`.t-jibun`)
3. 선택 해제 X 버튼 — 44px 원형, 우상단
4. **총액 블록** — `--accent-50` 바탕 · `--r-md`. 선을 긋지 않고도 요약이 한 덩어리로 읽힌다
   - 총액 `.t-amount` — 화면에서 가장 큰 숫자
   - 기준연도 `badge badge-accent`. **별도 줄에 둔다** (총액 옆에 두면 값 길이에 따라 줄바꿈이 들쭉날쭉해진다)
   - ㎡당 / 평당 단가 — 한 줄, `.t-label`

**탭** — `.tabs` + `.tab` (pill). 2차의 아래 2px 먹선을 대체한다.

| 탭 | 내용 |
|---|---|
| 땅 정보 | 지목 · 면적 · 소재지 · 고유번호(PNU) · 공시지가 안내 문구 |
| 시세 | 공시지가 연도별 추이(연도 수 가변) · 지역 실거래 중앙값 + **필수 경고 문구** |
| 주변 | 최근접 역 · 주변 시설 · 지역 뉴스(최하단, 바탕 한 단 낮춤) |

아코디언을 쓰지 않는 이유: 5개가 열리면 다시 세로 스크롤 문제로 돌아간다.

---

## 절대 어기면 안 되는 것

### 실거래가를 필지 단위로 표시하지 않는다

개인정보 보호로 지번이 마스킹되어(`245-*`) 제공되므로 특정 필지의 실거래가를 아는 것은
**원천적으로 불가능하다.** 지역 평균으로만 표시하고 아래 문구를 반드시 함께 노출한다.

```
이 필지의 거래 기록이 아닙니다.
정부가 지번을 일부만 공개해 지역 평균으로만 보여드립니다.
```

이 안내를 빼면 사용자가 자기 땅이 그 값에 팔렸다고 오해한다. 크기는 작게, 문구는 반드시.

### 공시지가는 시세가 아니다

연 1회(매년 1월 1일 기준) 공시되는 공적 가격이다. 실거래가의 절반~70% 수준이다.
화면에 **기준연도를 반드시 명시**한다 (`2026년 공시`).
단, **같은 화면에 두 번 적지 않는다** — 아이브로가 연도를 말하면 배지는 붙이지 않는다.

### 데이터가 없는 항목은 배지를 붙인다

공공시설·뉴스는 아직 데이터가 없다. **배지 없이 그럴듯한 수치를 노출하지 않는다.**

---

## 지도 표현 — 2차에서 바뀌지 않았다

- 필지 채움 투명도 `0.72`. 밑의 지형이 비쳐야 위치를 파악할 수 있다
- 필지 경계선 `#FFFFFF` 0.5px. 먹선으로 하면 축소 시 화면이 새까매진다
- 선택된 필지: `--ink` 2px 테두리 + 투명도 `1.0`
- 호버: 테두리 1px `--ink`, 커서 pointer
- z15 미만: 폴리곤을 그리지 않는다. **안내 문구도 띄우지 않는다** — 지도를 덮는 배너가 거슬린다.
  대신 읍면 시세 칩이 그 구간의 콘텐츠가 된다
- **가격 칩**은 `--r-md` + `--e1`. `overflow-hidden`이 있어야 왼쪽 색 띠가 라운드에 맞게 잘린다.
  값은 `.tnum font-bold`, 크기는 시군구 22px · 읍면동 17px (타입 스케일 안에서 고른다)
- **지도 위 토스트**(불러오기 실패 / 결과 초과 / 로딩)는 `--r-full` pill + `--e2`.
  위치는 상단 중앙 — 좌우 카드 사이다

### 베이스맵 — VWorld `Base` + `Hybrid` 2레이어

`Base` 한 장에는 배경(육지·물)과 도로·지명이 함께 그려져 있다. 래스터라 일부만 골라낼 수 없어
배경을 밝히면 도로까지 흐려진다. `Hybrid`(투명 오버레이)를 얹어 둘을 각각 조절한다.
값은 `web/lib/basemap.ts`에 있고 **3차에서 바꾸지 않았다** (`raster-saturation: -0.7`).

구현 주의 세 가지 (실제로 겪은 것):

- **워커를 직접 서빙해야 한다.** MapLibre는 GeoJSON 파싱을 웹 워커에서 하는데 Next.js 번들을 거치면
  워커 경로가 깨져 **필지만 조용히 사라진다**(배경지도는 정상). `public/`으로 복사하고 `setWorkerUrl()`로 가리킬 것
- 타일 좌표가 **`{z}/{y}/{x}` 순서**다. 표준 XYZ와 x·y가 뒤바뀌어 있다
- **CSS `filter: grayscale()`을 쓰지 말 것.** MapLibre는 베이스맵과 필지를 같은 WebGL 캔버스에 그린다.
  캔버스에 CSS 필터를 걸면 가격 램프 색까지 함께 죽어 설계 명제가 무너진다

네이버·카카오 지도는 약관상 사용 불가다. OSM 공식 타일 서버도 실서비스 사용이 금지된다.

---

## 문구 규칙 — 2차에서 바뀌지 않았다

| 규칙 | 쓴다 | 쓰지 않는다 |
|---|---|---|
| 금액 | `3억 2,180만원` | `321,800,000원` |
| 면적 | `1,652㎡ (500평)` | `1,652㎡` |
| 지목 | `전(밭)` `임야(산)` `대(집터)` | `전` `임` `대` |
| 단가 | 평당 먼저, ㎡당 뒤에 | ㎡당만 |
| 거리 | `직선 1.8km (차 7분 추정)` | `도보 25분` |

- 문장형 종결 대신 명사형. 버튼은 동사로 (`검색`, `지도에서 보기`)
- 라벨·버튼은 문장부호로 끝내지 않는다. 안내 문장은 마침표를 찍는다
- **사과하지 않는다.** 무엇이 일어났고 무엇을 하면 되는지만 쓴다

```
검색 결과 없음: "'서종면 999-9'에 해당하는 필지가 없습니다. 지번을 확인해 주세요"
로딩 실패:     "필지 정보를 불러오지 못했습니다. 다시 시도" (버튼 포함)
빈 상태:       "지도에서 땅을 눌러 보세요"
결과 초과:     "필지가 많아 일부만 표시됩니다. 더 확대해 주세요"
```

---

## 품질 기준

- 데스크톱 전용. 1280px 미만에서 레이아웃을 줄이지 않고 가로 스크롤한다
- 14px 미만 텍스트가 없다 (지도 출처 표기 제외)
- 키보드 Tab 이동 시 포커스 링이 보인다. `overflow:hidden` 카드 안에서 잘리지 않는다
- `prefers-reduced-motion` 존중
- 다크모드는 구현하지 않는다
- 애니메이션은 **카드 등장(180ms) · 버튼 · 토글 노브(140ms) · 탭 전환(140ms)** 넷만
- 서체 요청이 `fonts.googleapis.com`으로 나가지 않는다 (자체 호스팅)
