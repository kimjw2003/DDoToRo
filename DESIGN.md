# 디자인 지침 (2차)

화면 작업 시 이 문서를 먼저 읽는다. 색상값·서체·간격은 여기서 정한 것만 쓴다. 임의로 추가하지 않는다.
구현체는 `web/app/globals.css`이고, 이 문서와 값이 다르면 globals.css가 맞다.
1차 지침은 `DESIGN-v1.md`로 남겨 뒀다 — **참고용이며 기준이 아니다.**

1차 문서에서 **살린 것**: 지도 표현 수치, VWorld 2레이어 구성, MapLibre 구현 주의, 문구 규칙.
**바꾼 것**: 색·서체 전체, 타입 스케일, 패널 구조, 레이아웃.
**뺀 것**: 모바일 하단 시트 — 데스크톱 확정 후 착수한다.

---

## 설계 명제 3개

셋이 충돌하면 위에 있는 것이 이긴다.

### 1. 채도는 가격만 뜻한다

UI 크롬·텍스트·아이콘은 전부 무채색이다. 채도가 있는 색은 **가격 램프 · 주 버튼 · 토글 켜짐** 셋뿐이고,
강조색은 램프 최상단(`--p5`)과 같은 색족이라 화면에 색 이야기가 하나만 존재한다.
필지 선택 상태는 색이 아니라 **먹선 굵기**로 표현한다.

2차에서 지하철·공공시설·뉴스가 들어오지만 **카테고리에 색을 배정하지 않는다.**
1.7px 단선 아이콘(`currentColor`)으로 구분한다. 이 명제는 유지된다.

이유: 이 서비스가 파는 것은 가격 정보 하나다. 색을 여기저기 쓰면 사용자 눈이 어디를 봐야 할지 모른다.
경쟁 서비스(밸류맵, 디스코)가 어렵다는 평을 듣는 이유가 정확히 이것이다.

### 2. 숫자는 명조로 인쇄된다

토지대장·등기부등본이 명조로 인쇄된다. 공시지가가 시세가 아니라 **공적으로 고시된 가격**임을
서체로 전달한다. 장식이 아니라 출처 표시다.

### 3. 모양으로 알아본다

필지 실루엣이 유일한 시그니처다. 땅은 하나하나 모양이 다르고 소유자는 자기 땅을 모양으로 알아본다.
아파트 동호수와 결정적으로 다른 점이다. 대담함은 여기 한 곳에만 쓴다.

---

## 색

토큰 이름은 1차와 같다. **값만 바뀌었으므로 컴포넌트의 `bg-paper` / `text-ink` / `border-line`은 그대로 둔다.**
새로 추가된 것은 `--line-strong` / `--sunken` / `--accent` / `--accent-ink` 넷이다.

| 토큰 | 값 (oklch) | 용도 |
|---|---|---|
| `--ink` | `20% 0.018 70` | 본문, 제목, 선택 테두리 |
| `--ink-mid` | `48% 0.012 70` | 보조 텍스트, 라벨 |
| `--ink-soft` | `56% 0.012 70` | 힌트, 단위 |
| `--paper` | `98% 0.004 95` | 화면 배경 |
| `--surface` | `100% 0.002 95` | 패널 배경 |
| `--line` | `90% 0.006 95` | 1px 구분선 |
| `--line-strong` | `82% 0.008 95` | 입력 테두리, 경고 박스 |
| `--sunken` | `96% 0.005 95` | 배지 바탕, hover |
| `--accent` | `52% 0.10 28` | 주 버튼 · 토글 켜짐 |

`--ink-soft`는 1차의 `#9a9a93`에서 올렸다. 14px 본문에서 대비가 4.5:1에 못 미쳤다.

### 가격 램프 5단계

| 토큰 | 값 | 구간 (원/㎡) |
|---|---|---|
| `--p1` | `95% 0.022 48` | ~ 21,300 |
| `--p2` | `88% 0.055 42` | ~ 42,400 |
| `--p3` | `78% 0.092 38` | ~ 77,100 |
| `--p4` | `64% 0.120 32` | ~ 156,000 |
| `--p5` | `46% 0.110 28` | 156,000 ~ |
| `--p-none` | `92% 0.004 95` | 정보 없음 (0.448%) |

- **1차의 초록 램프를 점토·벽돌로 교체했다.** VWorld `Base` 타일의 산림 초록과 램프가 같은 색상이라
  z13~14에서 필지와 지형이 섞였다. 벽돌 계열은 누런 육지·초록 산림·파란 물 어디와도 겹치지 않는다
- 단일 색상 램프다. **무지개 팔레트 금지** — 순서 정보가 사라진다
- 구간은 **분위수 5분할**. 등간격 금지 — 지가 분포가 극단적으로 치우쳐 있어 등간격으로 나누면
  대부분이 1단계에 몰린다
- 가격이 `null`인 필지는 **45° 사선 해치**(`.hatch-none`)로 칠하고 범례에 "정보 없음"으로 표기한다.
  색을 하나 더 늘리지 않는다
- `--p4`·`--p5` 위에는 흰 글씨, `--p1`~`--p3` 위에는 `--ink`

### 금지

- **강조색을 쓰는 곳은 셋뿐이다** — 주 버튼(화면당 1개) · 토글 켜짐 · 가격 램프 최상단.
  토글은 한 그룹 안에서 개수를 제한하지 않는다(같은 목록이라 하나의 덩어리로 읽힌다). 링크는 밑줄로 표현한다
- 경고·주의를 노랑/빨강으로 칠하지 않는다. `--line-strong` 테두리 박스 + `--ink-mid` 텍스트로 충분하다
- 그라디언트·글로우 금지. 그림자는 주 버튼의 1px 하단 입체에만
- **라운드 금지.** 유일한 예외가 토글이다 — 둥근 트랙 + 원형 노브.
  버튼·배지·카드·입력·탭은 각진 채로 둔다

---

## 서체

```css
--font-ui:   'IBM Plex Sans KR', 'Apple SD Gothic Neo', 'Malgun Gothic', sans-serif;
--font-num:  'Hahmlet', 'Noto Serif KR', 'Apple SD Gothic Neo', serif;
--font-mono: 'IBM Plex Mono', ui-monospace, Menlo, monospace;
```

`layout.tsx`에서 `<link>`로 불러온다 (globals.css에서 `@import` 금지 — Tailwind 전개 규칙 뒤로 밀린다).

```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Hahmlet:wght@400;500;600&family=IBM+Plex+Sans+KR:wght@400;500;600&family=IBM+Plex+Mono:wght@400;500&display=swap" rel="stylesheet">
```

| 역할 | 2차 | 1차 |
|---|---|---|
| 표제·지번·금액 | Hahmlet | 나눔명조 |
| 본문·라벨·버튼 | IBM Plex Sans KR | Pretendard |
| 고유번호·배지·아이브로 | IBM Plex Mono | (없음) |

명조를 쓰는 곳은 **총액 · 지번 · 상세 페이지 h1** 세 군데뿐이다 (`.font-serif-num`).
나머지 숫자는 Plex Sans + `.tnum`.

### 타입 스케일

| 용도 | 크기 | 굵기 | 자간 | 서체 |
|---|---|---|---|---|
| 공시지가 총액 | 40px | 500 | -0.025em | 명조 |
| 페이지 제목 | 30px | 500 | -0.02em | 명조 |
| 지번 | 22px | 500 | -0.01em | 명조 |
| 섹션 제목 | 17px | 600 | 0 | UI |
| 본문·탭·값 | 16px | 400 | 0 | UI |
| 라벨·단위·안내 | 14px | 400 | 0.012em | UI |
| 아이브로(라틴 대문자) | 14px | 500 | 0.09em | Mono |

- **14px 미만을 쓰지 않는다.** 주 사용자가 40~60대다.
  1차 지침은 하한을 선언하고도 단위·힌트에 13px을 썼다 — 전부 14px로 올린다.
  유일한 예외는 지도 출처 표기(`.maplibregl-ctrl-attrib`, 12px)다
- 굵기는 400 / 500 / 600 세 단계. **700은 쓰지 않는다**
- 본문 `line-height: 1.7`, 30px 이상 표제는 1.05~1.15
- 클릭 타깃 최소 44px
- 라틴 대문자에는 `letter-spacing: 0.09em`을 반드시 붙인다

---

## 시그니처: 필지 실루엣

상세 상단에 그 필지의 실제 모양을 SVG로 그린다. **88×88px** (1차 72px에서 키웠다).
단독 페이지에서는 104px.

- 채움: 그 필지의 가격 단계 색 (정보 없음이면 사선 해치)
- 테두리: `--ink` 1.25px, `vector-effect: non-scaling-stroke`
- geometry는 이미 API 응답에 있으므로 추가 요청 불필요
- MultiPolygon이면 가장 큰 폴리곤만 사용
- 정사각 viewBox에 정규화하되 종횡비는 유지한다 (찌그러뜨리면 모양 식별이 깨진다)

이것 말고 다른 장식은 넣지 않는다.

---

## 브랜드 마크

확정 마크는 **ㄸ · 먹 + 벽돌 · 라운드**다. 서비스 이름의 첫 자음이고, 두 획 사이의 흰 틈은
필지를 가르는 길이다. 시안과 근거는 `design/brand-mark-ddieut.html`,
실제 파일 확인은 `design/brand-assets.html`에 있다.

| 파일 | 쓰는 곳 |
|---|---|
| `web/app/icon.svg` | 파비콘. Next.js가 파일 이름만으로 자동 인식한다 |
| `web/public/brand/mark.svg` | **주 마크.** 헤더 등 밝은 바탕 |
| `web/public/brand/mark-reverse.svg` | 먹·벽돌 바탕 위 |
| `web/public/brand/mark-solid.svg` | 한 색만 되는 자리 |
| `web/public/brand/mark-mono.svg` | 흑백 문서 |
| `web/public/brand/app-icon.svg` | 512×512. 180×180 PNG로 변환해 `app/apple-icon.png` |
| `web/public/brand/og-image.svg` | OG **원본**. 그대로 배포하지 않는다 |

### 규격

```
viewBox        0 0 32 32
각 ㄷ          11.6 × 20 · 획 두께 3.6
모서리·획 끝   round (반지름 1.8 = 획 두께의 절반)
두 획 사이 틈  3.6 — 획 두께와 같다. 이 비례를 바꾸지 않는다
색             왼쪽 #1B150D(ink) · 오른쪽 #9A5048(accent)
```

가로획이 12.4 / 27.6에서 끊기는 것은 **의도된 값이다.** 둥근 캡이 1.8을 더 뻗어
원래 폭 14.2 / 29.4에 정확히 닿는다. 캡을 butt로 바꾸면 마크가 짧아진다.

### 운용

- 여백은 사방으로 획 두께 1개분. 최소 크기 16px, 그 이하는 바탕 있는 파비콘
- 기울이기 · 투명도로 흐리기 · 시스템 밖의 색 · 획 두께 변경 · 틈 넓히기 전부 금지
- 사진 위에 얹지 않는다
- **에셋 SVG의 색은 hex로 굳혀 두었다.** 파비콘·OG는 브라우저 크롬이나 외부 크롤러가
  렌더하므로 `oklch()`나 CSS 변수를 넣으면 색이 빠진다. 제품 화면 안에서 쓸 때만 토큰을 참조한다
- **워드마크(마크 + `DDoToRo`)는 이미지로 만들지 않는다.** HTML로 조판한다 —
  SVG에 텍스트를 넣으면 폰트가 없는 환경에서 깨진다.
  마크 32px + 이름 Hahmlet 600, 사이 간격 12px. 부제는 1px 세로선으로 나눈다
- OG는 `app/opengraph-image.tsx`에서 PNG로 생성한다. 대부분의 SNS 크롤러가 SVG를 받지 않는다

---

## 컴포넌트 — 토글

레이어 표시처럼 **즉시 적용되는 켬/끔**에만 쓴다. 확인 버튼을 눌러야 반영되는 설정은 체크박스다 —
토글은 손을 떼는 순간 적용된다는 약속이다.

켜짐은 **강조색으로 채운다.** 주 버튼과 같은 색이고 램프 최상단(`--p5`)과 같은 색족이라
화면의 색 이야기는 여전히 하나다. 초록·파랑처럼 시스템 밖의 색은 쓰지 않는다 — 사용자가 가격 신호로 오독한다.
형태는 **둥근 트랙 + 원형 노브**이며, 이 시스템에서 라운드를 쓰는 유일한 요소다.

| 항목 | 값 |
|---|---|
| 트랙 | 52 × 30px · `border-radius: 999px` · 1px 테두리 |
| 노브 | 24px 정원 · 이동거리 22px |
| 꺼짐 | 트랙 `--surface` · 테두리 `--line-strong` · 노브 `--ink-mid` |
| 켜짐 | 트랙 `--accent` · 테두리 `--accent` · 노브 `--surface` |
| hover | 꺼짐은 테두리만 `--ink`으로. 켜짐은 `--accent-ink`로 어두워진다 |
| focus | `outline: 2px solid var(--ink)` · offset 2px |
| 사용 불가 | 트랙 `--sunken` · 노브 `--line` · 라벨 `--ink-soft` |
| 행 높이 | 최소 44px. **행 전체가 클릭 영역**이다 |
| 애니메이션 | 노브 `transform 140ms ease` · 트랙 `background 140ms ease` |
| 마크업 | `<input type="checkbox" role="switch">` — JS 없이 동작하고 스크린리더가 읽는다 |

- 토글만 44px로 키우지 않는다. 트랙이 커지면 장난감처럼 보인다 — 타깃은 행으로 확보한다
- 라벨만으로 "끄면 무엇이 사라지는지" 알 수 없으면 14px `--ink-mid` 보조 설명을 반드시 붙인다
- 구현체는 `design/assets/tokens.css`의 `.switch` / `.switch-row`다.
  Tailwind로 옮길 때 값을 다시 정하지 말고 그대로 옮긴다

**양쪽이 모두 이름을 가진 값이면 토글이 아니라 2분할 세그먼트다.**
면적 단위(평 / ㎡)처럼 둘 다 정당한 선택지일 때는 둘 다 보여준다. 토글은 "끔"이 무엇인지
라벨 하나로 유추해야 하므로 켬·끔이 명확한 경우에만 쓴다.
세그먼트는 버튼 계열이라 **각진 채로 둔다** — 라운드 예외는 토글에만 적용된다.

---

## 레이아웃 — 데스크톱 전용

```
┌──────────────┬──────────────────────────────┐
│              │                      [줌 +−] │
│  검색 48px    │                              │
│  ──────────  │            지도               │
│  고정 요약    │                              │
│  (스크롤 X)   │                              │
│  ──────────  │  ┌──────────┐                │
│  탭 3개       │  │ 범례      │                │
│  탭 내용      │  └──────────┘                │
│  ──────────  │                              │
│  상세페이지 →  │                              │
└──────────────┴──────────────────────────────┘
   384px                  나머지 전부
```

- 패널 폭 **384px**, `≥1600px`에서 416px. 1차 320px에서 넓혔다 —
  지목·면적 2열과 5년 차트가 320px에서 눌린다
- 지도가 화면의 70% 이상을 차지한다
- **최소 캔버스 1280px.** 그보다 좁으면 축소하지 않고 가로 스크롤한다 (`.app-shell`).
  1024px로 낮추지 않는다 — 패널 384px를 빼면 지도가 640px밖에 안 남아 필지 클릭이 어려워진다
- 모바일은 이번 범위 밖이다. **지도 화면에** `@media (max-width: …)`로 패널을 접는 코드를 작성하지 말 것

**예외 하나 — `/land/[pnu]` 단독 페이지.**
이 페이지는 검색 유입이 목적이라 모바일 검색 트래픽이 그대로 들어온다. 앱 화면이 아니라 공개 문서다.
좁은 폭에서 레이아웃이 깨지지 않을 정도의 폴백(`max-width: 560px` 한 블록: 실루엣/제목 1열, 총액 36px)만
유지한다. **모바일 UI를 설계하는 것이 아니다** — 스냅 시트·햄버거·모바일 전용 컴포넌트를 만들지 말 것.

---

## 상세 패널 구조 — 이번 설계의 핵심

1차는 9블록을 세로로 쌓았다. 2차에서 항목이 5개 늘어 그 구조로는 감당이 안 된다.
**고정 요약 카드 + 3탭**으로 바꾼다.

```
┌─────────────────────────────┐
│ [실루엣 88] 읍면           × │  ← 고정. 절대 스크롤되지 않는다
│             지번 22px 명조   │
│ 3억 2,181만원                │
│ [2026년 공시]                │
│ ㎡당 194,800원 · 평당 …       │
├──────┬──────┬───────────────┤
│ 땅 정보│ 시세 │ 주변          │  ← 50px, 선택은 2px 먹선 + 굵기 600
├──────┴──────┴───────────────┤
│ (이 영역만 스크롤한다)         │
└─────────────────────────────┘
│ 이 땅의 상세 페이지 →         │  ← 고정
└─────────────────────────────┘
```

**고정 요약 카드** (순서를 바꾸지 않는다)
1. 필지 실루엣 88px
2. 주소 — `경기 양평군 서종면` (14px `--ink-mid`) / `문호리 245-7` (22px 명조)
3. 선택 해제 X 버튼 — 44px, 우상단
4. 공시지가 총액 — 40px 명조. 화면에서 가장 큰 숫자
5. 기준연도 — `2026년 공시` 배지. 별도 줄에 둔다 (총액 옆에 두면 값 길이에 따라 줄바꿈이 들쭉날쭉해진다)
6. ㎡당 / 평당 단가 — 한 줄, 14px

**탭 배분**

| 탭 | 내용 |
|---|---|
| 땅 정보 | 지목 · 면적 · 소재지 · 고유번호(PNU, mono) · 공시지가 안내 문구 |
| 시세 | 공시지가 연도별 추이(연도 수 가변) · 지역 실거래 중앙값 + **필수 경고 문구** |
| 주변 | 최근접 역 · 주변 시설 · 지역 뉴스(최하단, 바탕 한 단 낮춤) |

아코디언을 쓰지 않는 이유: 라벨이 모두 보이는 장점이 있으나 5개가 열리면 다시 세로 스크롤 문제로 돌아간다.
탭은 깊이를 3으로 묶어 어느 탭에서도 한 화면에 끝난다.
총액·지번·실루엣을 탭 밖에 고정하는 것이 핵심이다 — 어느 탭에 있어도 어느 땅인지 잃지 않는다.

---

## 절대 어기면 안 되는 것

### 실거래가를 필지 단위로 표시하지 않는다

개인정보 보호로 지번이 마스킹되어(`245-*`) 제공되므로 특정 필지의 실거래가를 아는 것은
**원천적으로 불가능하다.** 지역 평균으로만 표시하고 아래 문구를 반드시 함께 노출한다.

```
이 필지의 거래 기록이 아닙니다.
정부가 지번을 일부만 공개해 지역 평균으로만 보여드립니다.
```

이 안내를 빼면 사용자가 자기 땅이 그 값에 팔렸다고 오해한다. 크기는 작게, 문구는 반드시.

### 공시지가는 시세가 아니다

연 1회(매년 1월 1일 기준) 공시되는 공적 가격이다. 실거래가의 절반~70% 수준이다.
화면에 **기준연도를 반드시 명시**한다 (`2026년 공시`).

### 데이터가 없는 항목은 배지를 붙인다

지하철역 좌표·공공시설·뉴스는 아직 데이터가 없다.
**배지 없이 그럴듯한 수치를 노출하지 않는다.**

```
표시 예시 · 좌표 연동 전
외부 API 연동 전
```

---

## 지도 표현

- 필지 채움 투명도 `0.72`. 밑의 지형이 비쳐야 위치를 파악할 수 있다
- 필지 경계선 `#FFFFFF` 0.5px. 먹선으로 하면 축소 시 화면이 새까매진다
- 선택된 필지: `--ink` 2px 테두리 + 투명도 `1.0`
- 호버: 테두리 1px `--ink`, 커서 pointer
- 범례는 좌하단 고정, 가로 램프바 + 양끝 값 + 사선 해치 "정보 없음"
- z15 미만: 폴리곤을 그리지 않는다. **안내 문구도 띄우지 않는다** — 지도를 덮는 배너가 거슬린다.
  대신 읍면 시세 칩이 그 구간의 콘텐츠가 된다
- 지도 위 텍스트(지명·지번·시세 칩)는 줌해도 화면상 크기가 일정해야 한다.
  SVG/오버레이로 그린다면 `viewBox폭 ÷ 화면폭`을 곱해 보정한다

### 베이스맵 — VWorld `Base` + `Hybrid` 2레이어 (1차 구성 유지)

`Base` 한 장에는 배경(육지·물)과 도로·지명이 함께 그려져 있다. 래스터라 일부만 골라낼 수 없어
배경을 밝히면 도로까지 흐려진다. `Hybrid`(투명 오버레이)를 얹어 둘을 각각 조절한다.

```js
{ id: 'base', type: 'raster', source: 'base',
  paint: {
    // 2차 주의: 램프가 따뜻한 벽돌 계열로 바뀌었다.
    // VWorld의 누런 육지와 색족이 겹치므로 1차(-0.55)보다 더 눌러야 한다.
    // z13~14에서 눈으로 확인하고 조정할 것 — z15 이상에서는 필지가 덮어 차이가 안 보인다
    'raster-saturation': -0.7,
    'raster-contrast': 0,
    'raster-brightness-min': 0.5
  } }

{ id: 'roads', type: 'raster', source: 'roads',   // Hybrid 타일
  paint: { 'raster-saturation': 0.3, 'raster-opacity': 1 } }
```

구현 주의 세 가지 (1차에서 실제로 겪은 것):

- **워커를 직접 서빙해야 한다.** MapLibre는 GeoJSON 파싱을 웹 워커에서 하는데 Next.js 번들을 거치면
  워커 경로가 깨져 **필지만 조용히 사라진다**(배경지도는 정상). `public/`으로 복사하고 `setWorkerUrl()`로 가리킬 것
- 타일 좌표가 **`{z}/{y}/{x}` 순서**다. 표준 XYZ와 x·y가 뒤바뀌어 있다
- 타일 상한 z18. `maxzoom: 18`을 명시하면 그 이상은 확대해 쓴다
- **CSS `filter: grayscale()`을 쓰지 말 것.** MapLibre는 베이스맵과 필지를 같은 WebGL 캔버스에 그린다.
  캔버스에 CSS 필터를 걸면 가격 램프 색까지 함께 죽어 설계 명제가 무너진다.
  채도 조정은 반드시 래스터 레이어의 paint 속성으로 한다

네이버·카카오 지도는 약관상 사용 불가다. OSM 공식 타일 서버도 실서비스 사용이 금지된다.

---

## 문구 규칙

| 규칙 | 쓴다 | 쓰지 않는다 |
|---|---|---|
| 금액 | `3억 2,180만원` | `321,800,000원` |
| 면적 | `1,652㎡ (500평)` | `1,652㎡` |
| 지목 | `전(밭)` `임야(산)` `대(집터)` | `전` `임` `대` |
| 단가 | 평당 먼저, ㎡당 뒤에 | ㎡당만 |
| 거리 | `직선 1.8km (차 7분 추정)` | `도보 25분` |

- 문장형 종결 대신 명사형. 버튼은 동사로 (`검색`, `지도로 돌아가기`)
- 라벨·버튼은 문장부호로 끝내지 않는다. 안내 문장은 마침표를 찍는다
- **사과하지 않는다.** 무엇이 일어났고 무엇을 하면 되는지만 쓴다

```
검색 결과 없음: "'서종면 999-9'에 해당하는 필지가 없습니다. 지번을 확인해 주세요"
로딩 실패:     "필지 정보를 불러오지 못했습니다. 다시 시도" (버튼 포함)
빈 상태:       "지도에서 땅을 눌러 보세요"
결과 초과:     "필지가 많아 일부만 표시됩니다 · 3,000건"
```

---

## 품질 기준

- 데스크톱 전용. 1280px 미만에서 레이아웃을 줄이지 않고 가로 스크롤한다
- 14px 미만 텍스트가 없다 (지도 출처 표기 제외)
- 키보드 Tab 이동 시 포커스 링이 보인다 (`outline: 2px solid var(--ink)`)
- `prefers-reduced-motion` 존중
- 다크모드는 구현하지 않는다
- 애니메이션은 패널 페이드인 · 주 버튼 1px 눌림 · 토스트 페이드 · 토글 노브 이동(140ms) 넷만
