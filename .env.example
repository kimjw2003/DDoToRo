# 이 파일을 .env로 복사해 실제 값을 채운다. .env는 커밋되지 않는다.

# 국토교통부 토지 매매 실거래가 API (data.go.kr)
# 반드시 Decoding 키를 사용한다
RTMS_SERVICE_KEY=

# PostGIS (docker-compose)
PGHOST=localhost
PGPORT=5432
PGDATABASE=ddotoro
PGUSER=ddotoro
PGPASSWORD=
