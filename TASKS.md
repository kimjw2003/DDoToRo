# MVP 작업 지시서

Task는 순서대로 진행한다. **각 Task의 완료 조건(DoD)을 충족하기 전에 다음 Task로 넘어가지 않는다.**
한 Task = 한 세션 기준. 여러 Task를 한 번에 처리하지 말 것.

---

## Task 1 — 원본 데이터 구조 파악

**목표**: 개별공시지가 SHP에 무엇이 들어있는지 확인한다. 이 결과로 이후 설계가 갈린다.

**할 일**
1. `/etl` 디렉토리와 Python 가상환경 생성 (geopandas, pandas, psycopg[binary], requests)
2. `/etl/data`에 있는 개별공시지가 SHP를 읽어 다음을 출력하는 스크립트 작성
   - CRS
   - 전체 컬럼명과 dtype
   - 상위 5행 샘플
   - geometry 타입 (Polygon / Point / 없음)
   - 전체 피처 수
3. 인코딩은 `cp949`로 시도하고, 실패 시 `euc-kr` → `utf-8` 순으로 폴백

**DoD**
- 지가 컬럼명, PNU 컬럼명, geometry 타입이 문서로 정리되어 있다
- 아래 분기 중 어느 쪽인지 결론이 나 있다

**분기**
- **A) Polygon + 지가 모두 존재** → 연속지적도를 사용하지 않는다. Task 2에서 단일 소스로 적재
- **B) 지가만 존재** → 연속지적도 SHP를 추가로 읽어 PNU 조인이 필요하다. Task 2에 조인 단계 추가

**주의**: 결과를 확인하기 전에 DB 스키마나 적재 코드를 작성하지 말 것.

---

## Task 2 — PostGIS 기동 및 데이터 적재

**목표**: 양평군 필지가 좌표와 가격을 갖고 DB에 들어간다.

**할 일**
1. `docker-compose.yml` 작성 (`imresamu/postgis:16-3.5`, 볼륨 영속화, 포트 5432)

   공식 `postgis/postgis`는 **amd64 전용**이라 Apple Silicon에서 네이티브 구동이 안 된다.
   `imresamu/postgis`는 공식 Dockerfile을 multi-arch(arm64 포함)로 빌드한 이미지다.
2. 스키마 작성 — Task 1 결과를 반영해 실제 컬럼에 맞출 것

```sql
CREATE TABLE parcel (
  pnu          CHAR(19) PRIMARY KEY,
  sido         TEXT,
  sigungu      TEXT,
  emd          TEXT,
  jibun        TEXT,           -- 표시용 지번 문자열 (예: '245-7')
  jimok        TEXT,           -- 지목 코드 또는 명칭
  area_sqm     NUMERIC(14,2),
  price_per_sqm BIGINT,        -- 원/㎡, 매칭 실패 시 NULL
  price_year   SMALLINT,
  geom         GEOMETRY(MultiPolygon, 4326)
);
CREATE INDEX idx_parcel_geom ON parcel USING GIST (geom);
CREATE INDEX idx_parcel_emd ON parcel (sigungu, emd);
```

3. 적재 스크립트 작성
   - **양평군(PNU 앞 5자리 = 41830)만 필터링**해서 적재
   - EPSG:5186 → EPSG:4326 변환
   - PNU는 문자열로 유지, 필요 시 zero-padding 보정
   - 배치 insert (한 건씩 커밋 금지)

**DoD**
- `SELECT count(*) FROM parcel` 결과가 출력되어 있다
- `price_per_sqm IS NULL` 비율이 리포트되어 있다 (5% 미만이면 정상)
- 무작위 20개 필지를 GeoJSON으로 `/etl/out/sample.geojson`에 저장했다

**검증 (필수)**
`sample.geojson`을 geojson.io 등에 올려 **실제 양평군 위치에 찍히는지 눈으로 확인한다.**
좌표계 변환 오류는 예외 없이 조용히 통과하므로 반드시 시각 확인이 필요하다.
바다나 아프리카에 찍히면 CRS 처리가 틀린 것이다.

---

## Task 3 — 실거래가 수집

**목표**: 양평군 토지 실거래를 읍면동 단위로 집계할 수 있다.

**할 일**
1. 실거래가 API 클라이언트 작성
   - 인증키는 `.env`에서 읽는다. 하드코딩 금지
   - `LAWD_CD=41830`, 최근 36개월치 순회
   - 호출 간 0.3초 sleep, 실패 시 3회 재시도
   - 0건 응답은 정상 처리 (에러 아님)
2. 결과를 `land_trade` 테이블에 적재

```sql
CREATE TABLE land_trade (
  id           BIGSERIAL PRIMARY KEY,
  sigungu_cd   CHAR(5),
  emd          TEXT,
  deal_ym      CHAR(6),
  deal_amount  BIGINT,     -- 원 단위
  area_sqm     NUMERIC(14,2),
  jimok        TEXT,
  raw          JSONB
);
```

3. 읍면동별 ㎡당 평균 거래가를 계산하는 뷰 또는 쿼리 작성

**DoD**
- 36개월 중 실제 데이터가 있는 개월 수와 총 건수가 리포트되어 있다
- 응답의 지번 필드가 마스킹되어 있음을 샘플로 확인했다
- 읍면동별 집계 결과가 출력되어 있다

**주의**: `raw` 컬럼에 원본 응답을 그대로 보관할 것. 필드명이 예상과 다를 때 재파싱할 수 있어야 한다.

---

## Task 4 — bbox 조회 API

**목표**: 지도 화면 영역에 해당하는 필지를 GeoJSON으로 받을 수 있다.

**할 일**
1. `/web`에 Next.js 15 프로젝트 생성 (TypeScript, Tailwind, App Router)
2. `GET /api/parcels?bbox=minLng,minLat,maxLng,maxLat&zoom=15`
   - zoom < 15 이면 빈 FeatureCollection과 `{ "too_far": true }` 반환
   - PostGIS `ST_MakeEnvelope` + `&&` 연산자 사용
   - 결과 상한 3000건, 초과 시 잘라내고 플래그 반환
   - geometry는 `ST_AsGeoJSON`으로 직렬화
3. `GET /api/parcels/[pnu]` — 단일 필지 상세
   - 필지 정보 + 해당 읍면동 실거래 집계를 함께 반환
   - 실거래는 반드시 지역 집계임을 나타내는 필드명으로 (`emd_trade_avg` 등)

**DoD**
- curl로 양평군 좌표 범위를 조회해 피처가 반환된다
- 응답 시간이 500ms 이내다 (초과 시 인덱스 확인)

---

## Task 5 — 지도 화면

**목표**: 지도에서 필지가 가격별로 색칠되고, 클릭하면 정보가 뜬다.

**할 일**
1. MapLibre GL JS + OSM 래스터 타일 베이스맵
2. 초기 위치는 양평군 중심, zoom 15
3. 지도 이동/줌 종료 시 bbox API 호출 (300ms 디바운스)
4. 필지 채색: ㎡당 가격 기준 5단계 **단일 색상 램프** (연함 → 진함). 무지개 팔레트 금지
5. zoom < 15 이면 폴리곤 대신 "확대해 주세요" 안내 표시
6. 필지 클릭 시 좌측 패널에 상세 표시

**좌측 패널 표시 순서 (이 순서를 지킬 것)**
1. 주소 (읍면동 + 지번)
2. **공시지가 총액** — 가장 크게. 예: `3억 2,180만원`
3. ㎡당 / 평당 단가 (작게)
4. 지목, 면적(㎡ + 평 병기), 기준연도
5. 이 지역 최근 실거래 평균 — **"이 필지의 거래가가 아님"을 명시**

**DoD**
- 지도에서 필지를 클릭하면 패널에 값이 채워진다
- 모바일 폭(375px)에서 패널이 하단 시트로 전환된다

---

## Task 6 — 검색과 상세 페이지

**목표**: 주소로 필지를 찾을 수 있고, 필지마다 고유 URL이 있다.

**할 일**
1. `GET /api/search?q=서종면 245-7`
   - 읍면동명 + 지번 파싱, `parcel` 테이블 LIKE 검색
   - 도로명주소는 MVP 범위 밖
2. 검색 결과 클릭 시 지도가 해당 필지로 이동하고 선택 상태가 된다
3. `/land/[pnu]` 서버 사이드 렌더링 페이지
   - 필지 정보 전체를 HTML로 렌더 (SEO)
   - `<title>`: `경기 양평군 서종면 245-7 공시지가 - DDoToRo`
   - `<meta description>`에 총액과 면적 포함
   - JSON-LD `Place` 구조화 데이터
   - 지도로 돌아가는 링크

**DoD**
- JS를 끈 상태로 `/land/{pnu}`에 접속해도 내용이 보인다
- 검색 → 지도 이동 → 상세 페이지 흐름이 끊기지 않는다

---

## 완료 후 확인

- [ ] 양평군 전체 필지가 지도에 뜬다
- [ ] 필지 클릭 → 총액이 보인다
- [ ] 검색이 동작한다
- [ ] `/land/[pnu]`가 SSR된다
- [ ] 모바일에서 사용 가능하다
- [ ] `price_per_sqm` NULL 비율이 5% 미만이다

여기까지가 MVP다. 도로접면(맹지 판별), 용도지역, 공시지가 시계열, 전국 확장은 **모두 이후 작업**이며 지금 착수하지 않는다.


# 2차 작업 지시서 (Task 7~14)

레포의 `TASKS.md` 뒤에 이어 붙인다. 1차와 같은 규칙이다.
Task는 순서대로 진행하고, **각 Task의 DoD를 충족하기 전에 다음으로 넘어가지 않는다.**
한 Task = 한 세션. 여러 Task를 한 번에 처리하지 말 것.

시안은 `design/` 폴더의 HTML이다. **정답지로 읽되 코드를 복사하지 않는다** —
바닐라 JS + 인라인 스타일로 짜여 있어 그대로 옮기면 레포가 망가진다.
구조·수치·문구를 옮기고 React + Tailwind로 다시 쓴다.

---

## Task 7 — 디자인 토큰 교체

**목표**: 기존 화면이 그대로 동작하면서 새 색·서체로 뜬다.

**할 일**
1. `web/app/globals.css`를 `design/globals.css`로 통째 교체
2. `web/app/layout.tsx`의 폰트 `<link>`를 교체
   - Pretendard / 나눔명조 제거
   - `Hahmlet:wght@400;500;600` + `IBM+Plex+Sans+KR:wght@400;500;600` + `IBM+Plex+Mono:wght@400;500`
3. `web/lib/priceRamp.ts`의 램프 색을 새 값으로 교체 (`--p1`~`--p5` 참조 방식이면 수정 불필요)
4. 13px을 쓰는 곳을 전부 찾아 14px로 올린다 (`grep -rn "text-\[13px\]\|13px" web/`)
5. 가격 `null` 필지를 단색 대신 `.hatch-none` 사선으로 칠한다

**DoD**
- `npm run dev`로 지도가 뜨고 필지가 벽돌 램프 5단계로 칠해진다
- 콘솔 에러 0
- 14px 미만 텍스트가 없다 (지도 출처 표기 `.maplibregl-ctrl-attrib` 12px만 예외)
- z13~14에서 베이스맵과 램프가 섞이지 않는다

**주의**
- 토큰 **이름**은 1차와 같다. `bg-paper` / `text-ink` / `border-line` 클래스명을 바꾸지 말 것.
  값만 바뀌므로 컴포넌트 수정은 최소여야 한다. 대규모 diff가 나오면 잘못하고 있는 것이다
- `raster-saturation`을 `-0.7`로 올렸다. z13~14에서 눈으로 확인하고 조정한다.
  z15 이상에서는 필지가 덮어 차이가 안 보인다

---

## Task 8 — 상세 패널 재구조

**목표**: 좌측 패널이 `고정 요약 카드 + 3탭`이 된다. 신규 데이터는 아직 없다.

**할 일**
1. 패널 폭 320 → **384px** (`≥1600px`에서 416px), 최상위에 `.app-shell` 적용
2. `web/components/ParcelPanel.tsx`를 다음 구조로 재작성
   - **고정 요약** (스크롤 영역 밖): 실루엣 88px · 주소 2행 · **X 버튼 44px** · 총액 40px 명조 ·
     기준연도 배지(별도 줄) · ㎡당/평당 단가
   - **탭 3개**: `땅 정보` / `시세` / `주변` — 높이 50px, 선택은 2px 먹선 + 굵기 600
   - **탭 내용만 스크롤**
   - **고정 푸터**: `이 땅의 상세 페이지 →`
3. `땅 정보` 탭에 기존 항목을 옮긴다 — 지목 · 면적 · 소재지 · 고유번호(PNU, mono) · 공시지가 안내 문구
4. `시세` / `주변` 탭은 이 Task에서 빈 상태로 둔다 (`데이터 연동 전` 배지)
5. `web/components/ParcelSilhouette.tsx`를 72 → 88px로. 종횡비는 유지한다
6. 탭 상태는 `useState`로만. URL에 넣지 않는다 (`?pnu=`만 유지)

**DoD**
- 필지를 클릭하면 요약이 뜨고 탭 3개가 전환된다
- 탭을 바꿔도 총액·지번·실루엣이 화면에서 사라지지 않는다
- X 버튼으로 선택이 해제되고 `?pnu=`가 URL에서 빠진다
- 1280px 폭에서 패널·지도가 눌리지 않는다

**주의**
- 컴포넌트를 새로 만들지 말고 기존 `ParcelPanel.tsx`를 고친다
- 아코디언으로 바꾸지 말 것. 5개가 열리면 다시 세로 스크롤 문제로 돌아간다

---

## Task 9 — 읍면별 현재 시세 오버레이 (z15 미만)

**목표**: 축소했을 때 읍면 12개 시세가 지도 위에 보인다. **데이터는 이미 있다.**

**할 일**
1. 읍면 실거래 중앙값 집계 API 추가 (`web/app/api/towns` 또는 기존 응답에 포함)
   - 읍면명 · ㎡당 중앙값 · 건수 · 대표 좌표(읍면 폴리곤 centroid 또는 상수 테이블)
2. `web/components/ParcelMap.tsx`에 칩 오버레이 추가 — z15 미만에서만 표시
   - **평당을 크게, `평당` 단위를 작게.** ㎡당은 쓰지 않는다 (일반인은 평으로 사고한다)
   - 줌해도 칩 크기는 화면상 일정해야 한다
3. 범례를 줌에 따라 전환한다 — z15+ 가격 램프 / z15 미만 `읍면별 현재 시세 · 최근 3년 실거래 중앙값 · 평당`

**DoD**
- z13~14에서 읍면 12개 칩이 뜨고, z15 이상에서 사라지며 필지가 나타난다
- 확대/축소해도 칩 글자 크기가 화면상 일정하다
- 양평읍 값이 실제 집계값(㎡당 229,585원 · 1,938건)과 일치한다

**주의**
- z15 미만에 "확대해 주세요" 배너를 띄우지 않는다. 칩이 그 구간의 콘텐츠다

---

## Task 10 — 공시지가 연도별 적재 (ETL)

**목표**: 2022~2026년 5년치 공시지가가 DB에 들어간다.

**할 일**
1. 원본 SHP의 `A16`~`A19` 컬럼(2022·2023·2024·2025년 공시지가)을 확인한다.
   **컬럼명이 실제로 그런지 먼저 출력해서 확인할 것**
2. 스키마 추가

```sql
CREATE TABLE parcel_price_history (
  pnu           CHAR(19) NOT NULL,
  price_year    SMALLINT NOT NULL,
  price_per_sqm BIGINT,
  PRIMARY KEY (pnu, price_year)
);
```

3. 적재 스크립트에서 현재 버리고 있는 과거 연도를 함께 적재
4. 필지 상세 API 응답에 `price_history: [{year, price_per_sqm}]` 추가

**DoD**
- `SELECT price_year, count(*) FROM parcel_price_history GROUP BY 1` 이 5개 연도를 출력한다
- 결측 비율이 연도별로 리포트되어 있다
- 2023년 값이 2022년보다 낮은 필지가 다수다 (그해 전국적으로 공시지가가 하락했다. 정상이다)

**주의**
- 10년치는 국토부에서 과거 공시지가를 추가로 받아야 한다. 이 Task 범위가 아니다.
  **연도 수가 늘어날 것을 전제로 스키마와 API를 짠다**

---

## Task 11 — 시세 탭

**목표**: `시세` 탭에 추이 차트와 지역 실거래가 들어간다.

**할 일**
1. 연도별 추이 차트 — 막대. **연도 수 가변**을 전제로 그린다 (5년으로 시작, 나중에 10년)
   - 채움은 `--p3`, 최신 연도만 `--p5`. 외곽선만으로 그리지 않는다
   - 값 라벨과 연도 라벨은 14px 이상. 좁아지면 줄어드는 SVG 텍스트로 만들지 말 것
   - 캡션: `2022년 대비 +N% · 단위 ㎡당 원` + `2023년은 전국적으로 공시지가가 하락한 해입니다.`
2. 지역 실거래 — 읍면 중앙값을 **평당 먼저** 크게, ㎡당·건수·기간을 작게
3. 아래 문구를 반드시 붙인다. 빼면 사용자가 자기 땅이 그 값에 팔렸다고 오해한다

```
이 필지의 거래 기록이 아닙니다.
정부가 지번을 일부만 공개해 지역 평균으로만 보여드립니다.
```

**DoD**
- 연도가 3개든 10개든 차트가 깨지지 않는다 (배열 길이를 하드코딩하지 않았다)
- 경고 문구가 실거래 값과 같은 화면에 항상 함께 보인다
- 공시지가가 `null`인 필지에서 탭이 빈 상태로 정상 처리된다

---

## Task 12 — 주변 탭 (역 · 공공시설)

**목표**: `주변` 탭에 최근접 역과 주변 시설이 거리순으로 뜬다.

**할 일**
1. 경의중앙선 역 좌표를 상수로 입력한다 (양평군 내 10개 미만 — 양수·신원·국수·아신·오빈·양평·원덕·용문 등)
2. PostGIS로 직선거리 계산 (`ST_Distance` + geography 캐스팅)
3. 표기: **직선거리를 주 표기**, 차 소요시간은 `차 N분 추정`으로 병기.
   **도보 시간을 쓰지 않는다** — 경로 API 약관 확인 전이고 산악 지형에서 오차가 크다
4. 공공시설은 공공데이터포털(전국 학교·의료기관) 또는 VWorld POI로 확보.
   카테고리 5종: 초등학교 · 보건지소 · 마트 · 관공서 · 버스정류장
5. **개수가 아니라 가장 가까운 하나의 거리**를 보여준다.
   시골 땅에서 "반경 500m 내 30개"는 의미가 없다
6. 카테고리 구분은 **색이 아니라 1.7px 단선 아이콘**(`currentColor`)으로 한다

**DoD**
- 필지를 바꾸면 거리가 다시 계산된다
- 데이터 미확보 항목에 `표시 예시 · 연동 전` 배지가 붙어 있다.
  **배지 없이 그럴듯한 수치를 노출하지 않는다**
- 아이콘에 채도가 있는 색을 쓰지 않았다

---

## Task 13 — 단독 페이지 개편

**목표**: `/land/[pnu]`가 새 디자인으로 뜨고 JS 없이 전부 보인다.

**할 일**
1. `design/land-detail-page.html` 구조로 재작성 — 실루엣 104px · h1 지번 명조 30px ·
   총액 블록 · 땅 정보 2열 · 공시지가 추이 · 지역 실거래 + 경고 문구 · 공시지가 안내
2. 차트를 **HTML/CSS 막대로** 그린다. SVG로 그리면 좁은 화면에서 글자가 14px 밑으로 줄어든다
3. `<title>` / `<meta description>`에 지번·지목·면적·총액을 넣는다 (검색 유입이 목적)
4. 인쇄 스타일 — 헤더·버튼 숨김

**DoD**
- 브라우저 JS를 끈 상태에서 모든 정보가 보인다
- 인쇄 미리보기가 1~2장으로 깔끔하게 나온다
- 지도로 돌아가면 선택 상태가 유지된다 (`?pnu=`)

---

## Task 14 — 지역 부동산 뉴스 (선택, 최후순위)

**목표**: `주변` 탭 최하단에 지역 뉴스가 뜬다.

**할 일**
1. 네이버 검색 API 등 외부 API 연동. 키워드는 **군 단위 + 읍면 필터**를 권한다
   (읍면 단위로만 검색하면 결과가 급감한다)
2. 개별 필지와 직접 관련이 없으므로 **비중을 낮춘다** — 탭 최하단, 바탕색 한 단 낮춤(`--paper`),
   제목 굵기 500 + `--ink-mid`
3. 응답 실패 시 섹션 자체를 숨긴다. 에러 배너를 띄우지 않는다

**DoD**
- 뉴스가 없거나 API가 실패해도 `주변` 탭의 나머지가 정상이다
- 뉴스 블록이 역·시설보다 시각적으로 앞서지 않는다

---

## Task 15 — 3차 디자인 (부드러운 UI) · 완료 2026-08-24

**목표**: 직방·다방 수준으로 부드러운 화면. 서체를 바꾸는 것이 출발점이었다.

**왜**: 2차는 의도적으로 딱딱했다(라운드 금지·그림자 금지·숫자는 명조).
직방·다방·호갱노노가 실제로 로드하는 서체를 확인해 보니 셋 다 **Pretendard**였고,
서체가 뒤섞인 디스코만 "어렵다"는 평을 듣는다. 방향은 명확했다.

**한 일**
1. 서체 3종 → **Pretendard Variable 하나**. 명조(`.font-serif-num`)·모노(`.eyebrow`) 삭제.
   `npm i pretendard` + `copy:fonts`로 자체 호스팅(동적 서브셋)
2. 토큰 재작성 — 강조색 9단 틴트 사다리(hue 28 고정, 600·700은 2차 값 유지),
   라운드 5단, 엘리베이션 3단, 타입 스케일 클래스(`.t-amount` 등)
3. 지도 화면을 **좌측 384px 고정 레일 → 지도 전면 + 떠 있는 카드**로 재구성.
   반쯤 만들어져 있던 모바일 시트(SNAP) 코드 제거
4. 줌 컨트롤 우상단 → **우하단**(패널 카드와 겹침), 버튼 29px → 44px
5. 카드가 필지를 가리는 문제 — `flyTo`에 `padding`, 클릭에 `panBy`(`nudgeIntoView`)
6. OG 이미지 폰트 조달 교체 (Google Fonts CSS API → 로컬 woff + `readFile`)
7. `DESIGN.md` 3차 재작성, 2차는 `DESIGN-v2.md`로 보존,
   `design/design-system-v3.html` 신규, 나머지 시안은 2차 아카이브로 표시

**걸렸던 것**
- `fetch(new URL(…, import.meta.url))`은 Turbopack에서 `file:` URL로 풀려 실패한다.
  "No fonts are loaded"로 빌드가 죽는다 — `readFile(join(process.cwd(), "assets/…"))`를 쓴다
- `--accent-50/100`을 `0.012 / 0.024`로 뒀더니 웜 뉴트럴 바탕 위에서 분홍으로 읽혔다.
  `0.007 / 0.016`으로 내렸다
- `components/ParcelMap 2.tsx`(Finder 중복본)가 양평 시절 상수를 참조해 타입 체크를 깨고 있었다. 삭제

**남은 것**
- 모바일 하단 시트 — 다음 Task. 이번에는 데스크톱만 확정했다

---

## Task 16 — 역 데이터 경기도 전역 (Task 12 보완) · 완료 2026-08-24

**목표**: `주변` 탭의 '가까운 역'이 경기도 어디서나 뜬다.

**왜**: Task 12에서 UI는 끝났지만 데이터가 `web/lib/stations.ts`의 경의중앙선 9개뿐이었다.
경기도로 넓힌 뒤에도 그대로여서, 복정역이 코앞인 성남시 복정동 필지가 빈 배열로 나왔다
(`STATION_MAX_M = 15km`가 오답은 막아주고 있었다).

**한 일**
1. `etl/fetch_stations.py` — Overpass에서 역 노드·route 관계·멤버 노드를 **한 번에** 받아
   좌표 근접(250m) 매칭으로 노선명을 붙인다. 703개 · 노선명 95%
2. `etl/sql/07_station.sql` · `export_sqlite.py`에 `station` 표와 `--stations-only` 추가
3. `push_turso.mjs --replace`가 원격에 없는 표를 만들어 주도록 보완
4. `web/lib/parcel.ts` — 상수 배열 → 테이블 조회(위도 프리필터 + 하버사인). `lib/stations.ts` 삭제
5. OSM 출처 표기 (ODbL)

**걸렸던 것**
- Overpass는 requests 기본 User-Agent에 **406**을 준다. UA를 붙여야 통과한다
- 질의를 셋으로 나눠 쏘면 **429**. 하나로 합쳤고 `--endpoint kumi` 미러를 두었다
- 노선명을 **이름으로 매칭하면 동명이역이 섞인다** — 양평역이 '5호선 · 경의중앙선'이 됐다.
  좌표 근접으로 바꿔 해결
- OSM은 환승역을 운영기관별로 따로 찍기도 한다(복정역 3m 간격 노드 2개). 500m·동명 병합
- 레일바이크 종점·선착장도 `railway=station`이다. 이름으로 걸러낸다

**남은 것**
- 적재분은 캐시된 응답으로 넣었다. Overpass 쿼터가 풀리면
  `python fetch_stations.py` 한 번으로 재현하면 된다 (결과는 같아야 한다)
- **주변 시설(학교·의료·마트·관공서·버스)은 여전히 미연동.** 카테고리마다 출처가 달라
  역보다 훨씬 큰 작업이다. `공공데이터 연동 전` 배지가 붙어 있다

---

## Task 17 — 주변 시설 (Task 12 완결) · 완료 2026-08-25

**목표**: `주변` 탭의 '주변 시설' 다섯 칸이 실제 거리로 채워진다.

**왜**: Task 12에서 자리만 만들고 값은 전부 `—`, `공공데이터 연동 전` 배지가 붙어 있었다.

**한 일**
1. `etl/overpass.py` — Overpass 클라이언트를 공용 모듈로 분리(UA·재시도·미러).
   406/429 교훈이 스크립트마다 흩어지지 않게. `fetch_stations.py`도 이걸 쓰도록 정리
2. `etl/fetch_facilities.py` — 다섯 카테고리를 한 질의로 받아 `poi` 테이블에 적재.
   초등학교 2,405 · 병원의원 5,586 · 마트편의점 13,788 · 관공서 2,842 · 버스정류장 48,067
3. `etl/sql/08_poi.sql` · `export_sqlite.py`에 `poi` 표와 `--poi-only`,
   `(kind, lat, lng, name)` 커버링 인덱스
4. `push_turso.mjs --replace`가 인덱스까지 옮기도록 보완
5. `web/lib/parcel.ts` — `nearbyFacilities()`. 다섯 카테고리를 `UNION ALL` 한 질의로 묶고
   역 조회와 `Promise.all`로 나란히 보낸다
6. `ParcelPanel.tsx` — 하드코딩 5종·배지 제거, 이름 + 거리 표시

**설계 결정**
- **개수가 아니라 최근접 하나의 거리.** 시골 땅에서 '반경 500m 내 30개'는 의미가 없다
- 카테고리별 탐색 반경을 다르게 뒀다(버스 5km · 마트 8km · 학교/병원 15km · 관공서 20km).
  하나로 잡으면 시골이 전부 빈칸이거나 조회가 무거워진다
- 라벨을 '보건지소' → '병원·의원'으로 바꿨다. 보건지소만 세면 시골에서도 대부분 빈칸인데,
  실제로 궁금한 것은 '아플 때 갈 곳이 얼마나 가까운가'다
- 1km 미만은 m로 표기한다(`280m`). 걸어갈 만한지 판단하기 쉽다

**걸렸던 것**
- 학교·병원·마트는 건물 폴리곤(way)이라 `out center` 없이는 절반이 사라진다
- `--replace`가 표만 만들고 인덱스를 안 옮겨 7만 행이 전체 스캔이 될 뻔했다
- `copy:fonts`가 `rm -rf` 후 92개를 복사하는 동안 브라우저가 폰트 404를 맞았다.
  임시 디렉토리에 받아 `mv`로 바꾸도록 고쳤다

**남은 것**
- 관공서에 마을회관이 섞인다(OSM이 `amenity=townhall`로 찍는다). 시골에서는 오히려 유용해 그대로 뒀다
