# 프로젝트: 토지 가격 조회 서비스 (MVP)

## 한 줄 요약
지도에서 필지를 클릭하면 공시지가·지목·면적을 보여주는 웹 서비스.
대상 지역은 **경기도 전체** (시군구 47개, 필지 5,210,962건). 전국 확장은 나중.

> 2026-07-30에 양평군(41830) 단일 시군구에서 경기도 전역으로 넓혔다.
> 원본 SHP가 애초에 시도 단위(AL_D150_**41**)라 추가 다운로드는 없었고,
> 코드에 박혀 있던 지역 상수를 걷어내는 것이 작업의 대부분이었다.

## 타겟 사용자
- 시골 땅을 상속받았거나 보유 중인데 가치를 모르는 사람
- 귀농/전원주택 부지를 찾는 사람

기존 서비스(밸류맵, 디스코)는 도심 빌딩·상가 중심이라 이 사용자층이 비어 있다.
따라서 UI는 **전문가용이 아니라 일반인용**. 정보 밀도보다 명료함이 우선.

---

## 반드시 알아야 할 도메인 지식

### PNU (필지고유번호) — 모든 데이터의 조인 키
19자리 문자열. **절대 숫자 타입으로 다루지 말 것** (선행 0 유실).

```
법정동코드(10) + 산여부(1) + 본번(4) + 부번(4)
예: 4183025029 1 0245 0007
    └ 양평군 서종면  일반  245번지 -7
```

- 산여부: `1` = 일반(토지), `2` = 산(임야)
- 본번/부번은 4자리 zero-padding
- DB 컬럼 타입은 `CHAR(19)`

### 개별공시지가
- 갱신 주기: **연 1회** (매년 1월 1일 기준, 5월경 공시)
- 단위: 원/㎡
- "실시간 시세"가 아니다. UI에 기준연도를 반드시 명시할 것
- 필지 총액 = 공시지가(원/㎡) × 면적(㎡)

### 토지 실거래가 — 결정적 제약
개인정보 보호로 **지번이 마스킹**되어 제공된다 (`245-*` 형태).
따라서 **실거래를 특정 필지에 매칭하는 것은 원천적으로 불가능**하다.

- 실거래는 **읍면동 단위 집계로만** 다룬다
- 필지 상세 화면에 "이 땅의 실거래가"를 표시하는 코드를 절대 작성하지 말 것
- 표시한다면 "이 지역 최근 실거래 평균" 형태로만

### 단위 표기
한국 사용자는 평 단위로 사고한다. ㎡ 표시 시 항상 평을 병기.
- 1평 = 3.3058㎡
- 예: `1,652㎡ (500평)`

### 데이터 품질
지적도와 공시지가는 기준일이 달라 조인이 100% 매칭되지 않는다(합병/분할 필지).
매칭 실패를 예외로 던지지 말고 `price = null`로 두고 카운트를 로깅할 것.

---

## 데이터 소스 (모두 확보 완료)

| 데이터 | 형태 | 상태 |
|---|---|---|
| 국토교통부_개별공시지가정보 | SHP (EPSG:5186, cp949) | 다운로드 완료 — **시도 단위 배포**라 `AL_D150_41_*` 하나에 경기도 전체가 들어 있다 (4.4GB, 6개 파일로 분할) |
| 국토교통부_연속지적_전국 | SHP | 조건부 (아래 참고) |
| 국토교통부_토지 매매 실거래가 자료 | 오픈API | 인증키 발급 완료 |
| 법정동코드 | CSV (code.go.kr) | 다운로드 |
| 베이스맵 | OSM 래스터 타일 | 신청 불필요 |

### 중요: 연속지적도 사용 여부는 미확정
개별공시지가 SHP에 Polygon geometry와 지가가 모두 들어있다면 연속지적도는 **사용하지 않는다**.
Task 1에서 이걸 먼저 확인한다. 확인 전에 조인 로직을 작성하지 말 것.

### 실거래가 API
```
GET https://apis.data.go.kr/1613000/RTMSDataSvcLandTrade/getRTMSDataSvcLandTrade
  serviceKey  발급키 (Decoding 키 사용)
  LAWD_CD     법정동코드 앞 5자리 (시군구 단위. 양평군 = 41830)
  DEAL_YMD    계약년월 6자리 (예: 202505)
```
군 단위 지역은 거래가 드물어 특정 월 0건이 정상이다. 에러로 처리하지 말 것.

**반드시 https로 부른다.** 같은 엔드포인트인데 http는 응답이 25초씩 걸려
30초 타임아웃에 간헐적으로 걸린다 (2026-08-07 실측: http 25.0s vs https 0.06s).

**LAWD_CD는 자치구 단위다.** 수원시는 41110이 아니라 41111(장안구)처럼 구별로
조회한다. 그래서 경기도 대상은 시군구 31개가 아니라 **47개**다.
원본 SHP의 A15 컬럼은 수원시 4개 구를 모두 41110으로 넣어두어 쓸 수 없다 —
시군구코드는 반드시 **PNU 앞 5자리**에서 얻는다.

**호출량과 계정.** 2026-08-07에 운영계정(100,000회/일)이 승인되어 한도는 더 이상
제약이 아니다. 전국으로 넓혀도 시군구 약 250개 × 36개월 ≈ 9,000회라 하루에 끝난다.
`fetch_trades.py`는 그래도 수집 이력(`trade_fetch_log`)을 남겨 이어받고, 한도 초과
코드(22)를 만나면 예외 대신 그때까지 받은 것을 남기고 멈춘다 — 그대로 둘 것.

**실거래는 한 번 받고 끝나는 데이터가 아니다.** 신고 기한이 계약 후 30일이라
최근 1~2개월치가 나중에 더 채워진다. 주기적으로 최근 몇 달을 다시 받는다:

```
python fetch_trades.py --refetch --months 3      # 47 × 3 = 141회
python export_sqlite.py --trades-only            # 실거래 표만 갱신 (1초)
node push_turso.mjs --local=file:<경로>/ddotoro-small.db \
     --replace=emd_trade_avg,sigungu_trade_avg   # 운영 반영
```

`--refetch`는 (시군구, 월) 단위로 지우고 다시 넣으므로 중복이 생기지 않는다.
공시지가는 연 1회라 이때 521만 필지를 다시 내보낼 필요가 없다.

---

## 기술 스택

| 영역 | 선택 |
|---|---|
| DB | PostgreSQL 16 + PostGIS 3 (Docker Compose, `imresamu/postgis:16-3.5`) |
| ETL | Python 3.11 (GeoPandas, pandas, psycopg) |
| 백엔드 | Next.js API Routes (별도 서버 없음) |
| 프론트 | Next.js 15 App Router + TypeScript |
| 지도 | MapLibre GL JS + OSM 래스터 타일 |
| 스타일 | Tailwind CSS |

### 스택 원칙
- **벡터타일(tippecanoe/PMTiles)을 MVP에서 만들지 않는다.** PostGIS bbox 쿼리로 GeoJSON 직접 서빙
  - 경기도 521만 필지에서 실측: bbox 조회 **24ms**. GiST 인덱스로 충분하며 벡터타일이 필요하지 않다
  - 다만 지역명 검색은 부분일치(`ILIKE '%…%'`)라 B-tree를 못 쓴다.
    trigram GIN 인덱스가 **없으면 1,063ms, 있으면 133ms**다 (`sql/06_search_index.sql`)
- z15 미만에서는 필지 폴리곤을 그리지 않는다. "확대해 주세요" 안내만 표시
- 상태관리 라이브러리 도입 금지. useState + URL 쿼리스트링으로 충분
- 좌표계: 원본 EPSG:5186 → DB 적재 시 **EPSG:4326으로 변환**해 저장

---

## 디렉토리 구조

```
/etl              Python 배치 스크립트
  /data           원본 SHP/CSV (gitignore)
  /out            검증용 샘플 출력 (gitignore)
/web              Next.js 앱
  /app
    /land/[pnu]   필지 상세 페이지 (SSR, SEO 핵심)
    /api
  /components
  /lib
docker-compose.yml
```

---

## 디자인

화면 작업 시 `DESIGN.md`를 반드시 먼저 읽는다. 색·서체·간격은 거기 정한 것만 쓴다.
값이 문서와 코드에서 다르면 **`web/app/globals.css`가 맞다.**

**현재 기준은 3차다** (2026-08-24). 2차에서 서체·형태·레이아웃이 통째로 바뀌었다.

| | 2차 | 3차 |
|---|---|---|
| 서체 | Hahmlet + IBM Plex Sans KR + Mono | **Pretendard Variable 하나** |
| 형태 | 라운드·그림자 금지 | **라운드 5단 + 엘리베이션 3단** |
| 지도 화면 | 좌측 384px 고정 레일 | **지도 전면 + 떠 있는 카드** |

`DESIGN-v2.md` / `DESIGN-v1.md`는 기록이며 **기준이 아니다.**

| 작업 | 볼 파일 |
|---|---|
| 색 · 서체 · 컴포넌트 | `design/design-system-v3.html` |
| 구현체(값의 출처) | `web/app/globals.css` |

`design/`의 나머지 시안(`map-desktop.html`, `land-detail-page.html`, `design-system.html`)은
**2차 아카이브다. 보고 구현하지 말 것** — 서체도 레이아웃도 지금 화면과 다르다.

### 서체를 바꿀 때 걸리는 것 셋

- Pretendard는 Google Fonts에 없다. `npm i pretendard` → `copy:fonts`가 `public/fonts/`로 복사한다
  (`copy:maplibre`와 같은 패턴, `predev`/`prebuild`가 실행). `public/fonts/`는 gitignore 대상이다
- **동적 서브셋을 통짜 파일로 바꾸지 말 것.** 92개 `@font-face`가 `unicode-range`로 쪼개져 있어
  한 페이지 전송량이 40~80KB로 끝난다. 통짜 variable은 woff2만 1.1MB다
- **OG 이미지는 별도 경로다.** satori가 woff2를 못 읽어 `web/assets/*.subset.woff`를 커밋해 두고
  `readFile(join(process.cwd(), "assets/…"))`로 읽는다. 경로를 리터럴로 적어야 빌드가 추적한다

2차 작업은 `TASKS.md`의 Task 7~14, 3차는 Task 15다. **한 세션에 한 Task씩만** 진행한다.

---

## 코딩 규칙

- 금액은 원 단위 정수로 저장. 화면 표시만 `억/만원` 포맷 (`3억 2,180만원`)
- 사용자 노출 문구는 한국어. 전문용어는 괄호로 풀어쓰기 (`지목: 전(밭)`)
- API 응답은 snake_case, 프론트 내부는 camelCase
- SHP 읽을 때 인코딩은 `cp949` (UTF-8로 읽으면 한글 깨짐)
- 커밋 접두어: `feat:` `fix:` `chore:`

---

## 하지 말 것

- 매물 등록/중개 기능 — 범위 밖
- 아파트 시세 — 범위 밖
- 로그인/회원가입 — MVP에 불필요
- 실거래가를 필지 단위로 매칭하는 로직 — 데이터상 불가능
- 다크모드 — 후순위
- **전국 데이터 적재 시도 — 경기도(41)까지만**
- 벡터타일 파이프라인 구축

### 지역명을 코드에 박지 말 것
경기도 확장에서 지운 하드코딩을 다시 만들지 않는다.

- 화면 문구의 지역명은 `web/lib/region.ts`의 `SERVICE_AREA` 하나만 쓴다
- 지도 초기 위치는 `basemap.ts`의 `DEFAULT_CENTER` (적재 데이터의 중심점 평균)
- 가격 램프 `PRICE_BREAKS`는 **적재 범위가 바뀌면 반드시 다시 계산한다**.
  양평군 기준 값을 경기도에 그대로 쓰면 도심 필지가 전부 최상위 색으로 뭉개진다
- ETL의 대상 시군구는 인자로 받는다. 상수로 두지 않는다
- 읍면동을 이름만으로 묶거나 조인하지 않는다 — 시군구를 넘으면 이름이 겹친다.
  반드시 `(sigungu_cd, emd)` 쌍을 쓴다

## 유의해야할 점
- 사용자에게는 존댓말 할것
