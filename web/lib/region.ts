/**
 * 서비스가 다루는 지역.
 *
 * 화면 문구와 외부 검색 질의에 쓴다. 범위를 넓히면 여기만 고친다 —
 * 지역명을 컴포넌트마다 적어두면 확장할 때 빠뜨리는 곳이 반드시 생긴다.
 *
 * 지금 적재된 것은 경기도 전체(시군구 47개, 필지 5,210,962건)다.
 */
export const SERVICE_AREA = {
  /** 문장 안에서 쓰는 이름 */
  name: "경기도",
  /** 제목·헤더처럼 짧게 써야 하는 자리 */
  short: "경기",
} as const;
